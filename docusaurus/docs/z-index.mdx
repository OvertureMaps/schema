---
title: ''
slug: /
---

# Overture Schema Docs Preview

The contents of this site (minus this landing page) will become the contents of docs.overturemaps.org/schema when the PR is merged into `main`.

The root of docs.overturemaps.org/schema will look like: the [Overture Schema](schema) page.

Please have a look around to ensure that:

1. The schema [reference](schema/reference) pages look as you expect with no unresovled ref errors.
2. Any changes [attributes](schema/attributes/) pages render as expected.
