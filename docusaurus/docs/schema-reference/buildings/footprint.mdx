import CodeBlock from '@theme/CodeBlock';
import JSONSchemaViewer from "@theme/JSONSchemaViewer";
import generateResolverOptions from "@site/src/components/shared-libs/generateResolverOptions"
import yamlLoad from "@site/src/components/yamlLoad"
import FootprintSchema from "!!raw-loader!@site/docs/_schema/buildings/footprint.yaml";

import PolygonExample from "!!raw-loader!@site/docs/_examples/buildings/footprint-polygon.yaml";
import MultiPolygonExample from "!!raw-loader!@site/docs/_examples/buildings/footprint-multipolygon.yaml";
import MultipleNamesExample from "!!raw-loader!@site/docs/_examples/buildings/name-multiple.yaml";

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# footprint

Footprints describe real-world buildings, combining the geometry of the
building footprint with other known facts about the building.

<table>
  <tr>
    <th>Geometry Type</th>
    <td>Polygon or MultiPolygon</td>
  </tr>
  <tr>
    <th><code>theme</code></th>
    <td><code>buildings</code></td>
  </tr>
  <tr>
    <th><code>type</code></th>
    <td><code>footprint</code></td>
  </tr>
</table>

TODO: Buildings team, please write documentation here covering whatever
      aspects are judged to be most important.

---

*TODO: As one of the people, if not the person, responsible for the name
       building/footprint, I think footprint is a poor choice of name,
       and a better fit would be theme=buildings, type=building. In my
       mind footprint introduces conceptual confusion because, while the
       geometry represents the footprint of the building, the rest of
       the properties don't really have anything to do with the footprint
       per se, they are about the whole building. It really hits home
       when you try to write a sentence describing the feature type
       which begins by "Footprints are ______. Can we change it? Sorry.
       --Vic 2023-06-02*

---

## Schema

<Tabs>
  <TabItem value="browsable" label="Browsable" default>
    <JSONSchemaViewer schema={ yamlLoad(FootprintSchema) } resolverOptions={ generateResolverOptions({remote: true, yamlBasePath: '/buildings'})}/>
  </TabItem>
  <TabItem value="yaml" label="YAML" default>
    <CodeBlock language="jsx">{FootprintSchema}</CodeBlock>
  </TabItem>
</Tabs>

## Examples

<Tabs>
  <TabItem value="1" label="Polygon Building" default>
    <CodeBlock language="json">{ JSON.stringify(yamlLoad(PolygonExample), null, 2) }</CodeBlock>
  </TabItem>

  <TabItem value="2" label="MultiPolygon Building" default>
    <CodeBlock language="json">{ JSON.stringify(yamlLoad(MultiPolygonExample), null, 2) }</CodeBlock>
  </TabItem>

  <TabItem value="3" label="Building with Multiple Names" default>
    <CodeBlock language="json">{ JSON.stringify(yamlLoad(MultipleNamesExample), null, 2) }</CodeBlock>
  </TabItem>

</Tabs>
