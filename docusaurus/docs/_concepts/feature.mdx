import CodeBlock from '@theme/CodeBlock';
import JSONSchemaViewer from "@theme/JSONSchemaViewer";
import generateResolverOptions from "@site/src/components/shared-libs/generateResolverOptions"
import yamlLoad from "@site/src/components/yamlLoad"
import Schema from '!!raw-loader!@site/docs/_schema/schema.yaml';

# Feature

TODO: This page is going to be about "what is an Overture Feature" and
it will talk about the kind of main thingies about a Feature:

- Simple Geometry
- Data Convertibility
- Canonical GeoJSON
- YAML
- Common Properties and Common-ish properties:
    - ID
    - Theme and Type properties
    - Version, updateTime
    - Level, height, ...
    - Name
- GERS-related properties
- Extension properties

`schema.yaml`

<JSONSchemaViewer schema={ yamlLoad(Schema) } resolverOptions={generateResolverOptions(
    {
        remote:true,
        yamlBasePath: ""
    }
)}/>

## Full Schema YAML

<CodeBlock language="jsx">{Schema}</CodeBlock>
