import CodeBlock from '@theme/CodeBlock';
import Schema from "@site/static/schema/defs.json";
import JSONSchemaViewer from "@theme/JSONSchemaViewer";
import generateResolverOptions from "@site/src/components/shared-libs/generateResolverOptions"
import rawSchema from '!!raw-loader!@site/docs/yaml/defs.yaml';

# Definitions

`defs.yaml`

<JSONSchemaViewer schema={ Schema } resolverOptions={generateResolverOptions(
    {
        remote:true,
        basePath: "schema"
    }
)}/>

# Raw Schema YAML :

<CodeBlock language="jsx">{rawSchema}</CodeBlock>
