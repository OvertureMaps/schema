import CodeBlock from '@theme/CodeBlock';
import JSONSchemaViewer from "@theme/JSONSchemaViewer";
import generateResolverOptions from "@site/src/components/shared-libs/generateResolverOptions"
import yamlToJSON from "@site/src/components/yamlToJSON"
import SegmentSchema from '!!raw-loader!@site/docs/yaml/transportation/segment.yaml';

# Segment

 Segment is a LineString feature, somewhat analogous to an OSM way. It's representing the geometry of edges in the transportation network
 and carrying the majority of properties.

# Schema
<JSONSchemaViewer schema={ yamlToJSON(SegmentSchema) } resolverOptions={ generateResolverOptions({remote: true, basePath: '/transportation'})}/>

# Raw Schema YAML :

<CodeBlock language="jsx">{SegmentSchema}</CodeBlock>
