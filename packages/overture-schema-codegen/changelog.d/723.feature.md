Added a `stac-table-columns` codegen format rendering the STAC Table extension's `table:columns` from the models, with a gap log recording what a flat column list cannot hold. Field defaults and deprecation now carry through extraction so the renderer can report both.
