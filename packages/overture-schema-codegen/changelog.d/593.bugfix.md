Fixed RootModel entry points producing a spurious `root` column and going undocumented: they are now skipped from PySpark generation and documented as markdown type aliases (a self-referential RootModel now raises a clear error instead of recursing).
