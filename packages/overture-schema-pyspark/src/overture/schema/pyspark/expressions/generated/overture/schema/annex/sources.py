# This file is auto-generated by overture-schema-codegen. Do not edit.

"""Sources validation expression builders."""

from __future__ import annotations

from pyspark.sql import functions as F
from pyspark.sql.types import (
    ArrayType,
    DoubleType,
    LongType,
    MapType,
    StringType,
    StructField,
    StructType,
)

from overture.schema.pyspark.check import Check, CheckShape, ModelValidation
from overture.schema.pyspark.expressions.column_patterns import (
    array_check,
    map_keys_check,
    map_values_check,
    nested_array_check,
)
from overture.schema.pyspark.expressions.constraint_expressions import (
    check_array_max_length,
    check_array_min_length,
    check_bounds,
    check_enum,
    check_pattern,
    check_required,
    check_url_format,
    check_url_length,
    except_literals,
)


def _datasets_check() -> Check:
    return Check(
        field="datasets",
        name="required",
        expr=check_required(F.col("datasets")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_source_name_check() -> Check:
    return Check(
        field="datasets[].source_name",
        name="required",
        expr=array_check("datasets", lambda el: check_required(el["source_name"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_source_dataset_name_check() -> Check:
    return Check(
        field="datasets[].source_dataset_name",
        name="required",
        expr=array_check(
            "datasets", lambda el: check_required(el["source_dataset_name"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_data_url_required_check() -> Check:
    return Check(
        field="datasets[].data_url",
        name="required",
        expr=array_check("datasets", lambda el: check_required(el["data_url"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_data_url_url_format_check() -> Check:
    return Check(
        field="datasets[].data_url",
        name="url_format",
        expr=array_check(
            "datasets",
            lambda el: except_literals(
                el["data_url"], check_url_format(el["data_url"]), [""]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_data_url_url_length_check() -> Check:
    return Check(
        field="datasets[].data_url",
        name="url_length",
        expr=array_check(
            "datasets",
            lambda el: except_literals(
                el["data_url"], check_url_length(el["data_url"]), [""]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_data_url_archived_required_check() -> Check:
    return Check(
        field="datasets[].data_url_archived",
        name="required",
        expr=array_check(
            "datasets", lambda el: check_required(el["data_url_archived"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_data_url_archived_url_format_check() -> Check:
    return Check(
        field="datasets[].data_url_archived",
        name="url_format",
        expr=array_check(
            "datasets",
            lambda el: except_literals(
                el["data_url_archived"], check_url_format(el["data_url_archived"]), [""]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_data_url_archived_url_length_check() -> Check:
    return Check(
        field="datasets[].data_url_archived",
        name="url_length",
        expr=array_check(
            "datasets",
            lambda el: except_literals(
                el["data_url_archived"], check_url_length(el["data_url_archived"]), [""]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_url_required_check() -> Check:
    return Check(
        field="datasets[].license_url",
        name="required",
        expr=array_check("datasets", lambda el: check_required(el["license_url"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_url_url_format_check() -> Check:
    return Check(
        field="datasets[].license_url",
        name="url_format",
        expr=array_check(
            "datasets",
            lambda el: except_literals(
                el["license_url"], check_url_format(el["license_url"]), [""]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_url_url_length_check() -> Check:
    return Check(
        field="datasets[].license_url",
        name="url_length",
        expr=array_check(
            "datasets",
            lambda el: except_literals(
                el["license_url"], check_url_length(el["license_url"]), [""]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_url_archived_required_check() -> Check:
    return Check(
        field="datasets[].license_url_archived",
        name="required",
        expr=array_check(
            "datasets", lambda el: check_required(el["license_url_archived"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_url_archived_url_format_check() -> Check:
    return Check(
        field="datasets[].license_url_archived",
        name="url_format",
        expr=array_check(
            "datasets",
            lambda el: except_literals(
                el["license_url_archived"],
                check_url_format(el["license_url_archived"]),
                [""],
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_url_archived_url_length_check() -> Check:
    return Check(
        field="datasets[].license_url_archived",
        name="url_length",
        expr=array_check(
            "datasets",
            lambda el: except_literals(
                el["license_url_archived"],
                check_url_length(el["license_url_archived"]),
                [""],
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_type_check() -> Check:
    return Check(
        field="datasets[].license_type",
        name="required",
        expr=array_check("datasets", lambda el: check_required(el["license_type"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_text_check() -> Check:
    return Check(
        field="datasets[].license_text",
        name="required",
        expr=array_check("datasets", lambda el: check_required(el["license_text"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_license_attribution_check() -> Check:
    return Check(
        field="datasets[].license_attribution",
        name="required",
        expr=array_check(
            "datasets", lambda el: check_required(el["license_attribution"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_coverage_bbox_check() -> Check:
    return Check(
        field="datasets[].coverage_bbox",
        name="required",
        expr=array_check("datasets", lambda el: check_required(el["coverage_bbox"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_coverage_bbox_min_length_check() -> Check:
    return Check(
        field="datasets[].coverage_bbox_min_length",
        name="array_min_length",
        expr=array_check(
            "datasets", lambda el: check_array_min_length(el["coverage_bbox"], 4)
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_coverage_bbox_max_length_check() -> Check:
    return Check(
        field="datasets[].coverage_bbox_max_length",
        name="array_max_length",
        expr=array_check(
            "datasets", lambda el: check_array_max_length(el["coverage_bbox"], 4)
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_url_url_format_check() -> Check:
    return Check(
        field="datasets[].url",
        name="url_format",
        expr=array_check("datasets", lambda el: check_url_format(el["url"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_url_url_length_check() -> Check:
    return Check(
        field="datasets[].url",
        name="url_length",
        expr=array_check("datasets", lambda el: check_url_length(el["url"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_url_archived_url_format_check() -> Check:
    return Check(
        field="datasets[].url_archived",
        name="url_format",
        expr=array_check("datasets", lambda el: check_url_format(el["url_archived"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_url_archived_url_length_check() -> Check:
    return Check(
        field="datasets[].url_archived",
        name="url_length",
        expr=array_check("datasets", lambda el: check_url_length(el["url_archived"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_data_download_url_url_format_check() -> Check:
    return Check(
        field="datasets[].data_download_url[]",
        name="url_format",
        expr=nested_array_check(
            "datasets",
            lambda el: array_check(
                el["data_download_url"],
                lambda inner: except_literals(inner, check_url_format(inner), [""]),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_data_download_url_url_length_check() -> Check:
    return Check(
        field="datasets[].data_download_url[]",
        name="url_length",
        expr=nested_array_check(
            "datasets",
            lambda el: array_check(
                el["data_download_url"],
                lambda inner: except_literals(inner, check_url_length(inner), [""]),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_countries_check() -> Check:
    return Check(
        field="datasets[].countries[]",
        name="country_code_alpha2",
        expr=nested_array_check(
            "datasets",
            lambda el: array_check(
                el["countries"],
                lambda inner: except_literals(
                    inner,
                    check_pattern(
                        inner, "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
                    ),
                    ["Global"],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_build_source_check() -> Check:
    return Check(
        field="datasets[].build_source",
        name="enum",
        expr=array_check(
            "datasets",
            lambda el: check_enum(
                el["build_source"], ["OpenAddresses", "tf-data-platform"]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _datasets_update_type_check() -> Check:
    return Check(
        field="datasets[].update_type",
        name="enum",
        expr=array_check(
            "datasets",
            lambda el: check_enum(el["update_type"], ["continuous", "manual"]),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"datasets"}),
    )


def _license_priority_check() -> Check:
    return Check(
        field="license_priority",
        name="required",
        expr=check_required(F.col("license_priority")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"license_priority"}),
    )


def _license_priority_key_check() -> Check:
    return Check(
        field="license_priority{key}",
        name="pattern",
        expr=map_keys_check(
            "license_priority",
            lambda k: check_pattern(k, "^[A-Za-z0-9._+\\-]+\\z", label="pattern"),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"license_priority"}),
    )


def _license_priority_value_check() -> Check:
    return Check(
        field="license_priority{value}",
        name="bounds",
        expr=map_values_check(
            "license_priority", lambda v: check_bounds(v, ge=0, check_nan=False)
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"license_priority"}),
    )


def sources_checks() -> list[Check]:
    """All validation checks for sources."""
    return [
        _datasets_check(),
        _datasets_source_name_check(),
        _datasets_source_dataset_name_check(),
        _datasets_data_url_required_check(),
        _datasets_data_url_url_format_check(),
        _datasets_data_url_url_length_check(),
        _datasets_data_url_archived_required_check(),
        _datasets_data_url_archived_url_format_check(),
        _datasets_data_url_archived_url_length_check(),
        _datasets_license_url_required_check(),
        _datasets_license_url_url_format_check(),
        _datasets_license_url_url_length_check(),
        _datasets_license_url_archived_required_check(),
        _datasets_license_url_archived_url_format_check(),
        _datasets_license_url_archived_url_length_check(),
        _datasets_license_type_check(),
        _datasets_license_text_check(),
        _datasets_license_attribution_check(),
        _datasets_coverage_bbox_check(),
        _datasets_coverage_bbox_min_length_check(),
        _datasets_coverage_bbox_max_length_check(),
        _datasets_url_url_format_check(),
        _datasets_url_url_length_check(),
        _datasets_url_archived_url_format_check(),
        _datasets_url_archived_url_length_check(),
        _datasets_data_download_url_url_format_check(),
        _datasets_data_download_url_url_length_check(),
        _datasets_countries_check(),
        _datasets_build_source_check(),
        _datasets_update_type_check(),
        _license_priority_check(),
        _license_priority_key_check(),
        _license_priority_value_check(),
    ]


SOURCES_SCHEMA = StructType(
    [
        StructField(
            "datasets",
            ArrayType(
                StructType(
                    [
                        StructField("source_name", StringType(), True),
                        StructField("source_dataset_name", StringType(), True),
                        StructField("data_url", StringType(), True),
                        StructField("data_url_archived", StringType(), True),
                        StructField("license_url", StringType(), True),
                        StructField("license_url_archived", StringType(), True),
                        StructField("license_type", StringType(), True),
                        StructField("license_text", StringType(), True),
                        StructField("license_attribution", StringType(), True),
                        StructField(
                            "coverage_bbox", ArrayType(DoubleType(), True), True
                        ),
                        StructField("inception_date", StringType(), True),
                        StructField("url", StringType(), True),
                        StructField("url_archived", StringType(), True),
                        StructField(
                            "data_download_url", ArrayType(StringType(), True), True
                        ),
                        StructField("countries", ArrayType(StringType(), True), True),
                        StructField("coverage_description", StringType(), True),
                        StructField("data_layer_name", StringType(), True),
                        StructField("oa_path", ArrayType(StringType(), True), True),
                        StructField(
                            "address_levels", ArrayType(StringType(), True), True
                        ),
                        StructField("file_format", StringType(), True),
                        StructField("update_frequency", StringType(), True),
                        StructField("build_source", StringType(), True),
                        StructField("update_type", StringType(), True),
                        StructField(
                            "update_schedule", ArrayType(StringType(), True), True
                        ),
                        StructField("known_issues", StringType(), True),
                        StructField("notes", StringType(), True),
                        StructField("requires_attribution", StringType(), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField("license_priority", MapType(StringType(), LongType(), True), True),
    ]
)

ENTRY_POINT = "overture.schema.annex:Sources"

PARTITIONS: dict[str, str] = {}

MODEL_VALIDATION = ModelValidation(
    schema=SOURCES_SCHEMA,
    checks=sources_checks,
)
