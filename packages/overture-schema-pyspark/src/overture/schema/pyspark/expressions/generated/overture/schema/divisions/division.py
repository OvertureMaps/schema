# This file is auto-generated by overture-schema-codegen. Do not edit.

"""Division validation expression builders."""

from __future__ import annotations

from pyspark.sql import functions as F
from pyspark.sql.types import (
    ArrayType,
    BinaryType,
    DoubleType,
    IntegerType,
    MapType,
    StringType,
    StructField,
    StructType,
)

from overture.schema.pyspark.check import Check, CheckShape, FeatureValidation
from overture.schema.pyspark.expressions._schema_structs import (
    BBOX_STRUCT,
)
from overture.schema.pyspark.expressions.column_patterns import (
    array_check,
    check_struct_unique,
    nested_array_check,
)
from overture.schema.pyspark.expressions.constraint_expressions import (
    check_array_min_length,
    check_bbox_completeness,
    check_bbox_lat_ordering,
    check_bbox_lat_range,
    check_bounds,
    check_enum,
    check_forbid_if,
    check_geometry_type,
    check_json_pointer,
    check_linear_range_bounds,
    check_linear_range_length,
    check_linear_range_order,
    check_pattern,
    check_require_if,
    check_required,
    check_string_min_length,
    check_stripped,
)
from overture.schema.system.primitive import GeometryType


def _cartography_prominence_bounds_check() -> Check:
    return Check(
        field="cartography.prominence",
        name="bounds",
        expr=check_bounds(F.col("cartography.prominence"), ge=1),
        shape=CheckShape.SCALAR,
        root_field="cartography",
    )


def _cartography_prominence_bounds_check_1() -> Check:
    return Check(
        field="cartography.prominence",
        name="bounds",
        expr=check_bounds(F.col("cartography.prominence"), le=100),
        shape=CheckShape.SCALAR,
        root_field="cartography",
    )


def _cartography_min_zoom_bounds_check() -> Check:
    return Check(
        field="cartography.min_zoom",
        name="bounds",
        expr=check_bounds(F.col("cartography.min_zoom"), ge=0),
        shape=CheckShape.SCALAR,
        root_field="cartography",
    )


def _cartography_min_zoom_bounds_check_1() -> Check:
    return Check(
        field="cartography.min_zoom",
        name="bounds",
        expr=check_bounds(F.col("cartography.min_zoom"), le=23),
        shape=CheckShape.SCALAR,
        root_field="cartography",
    )


def _cartography_max_zoom_bounds_check() -> Check:
    return Check(
        field="cartography.max_zoom",
        name="bounds",
        expr=check_bounds(F.col("cartography.max_zoom"), ge=0),
        shape=CheckShape.SCALAR,
        root_field="cartography",
    )


def _cartography_max_zoom_bounds_check_1() -> Check:
    return Check(
        field="cartography.max_zoom",
        name="bounds",
        expr=check_bounds(F.col("cartography.max_zoom"), le=23),
        shape=CheckShape.SCALAR,
        root_field="cartography",
    )


def _names_check() -> Check:
    return Check(
        field="names",
        name="required",
        expr=check_required(F.col("names")),
        shape=CheckShape.SCALAR,
        root_field="names",
    )


def _names_primary_required_check() -> Check:
    return Check(
        field="names.primary",
        name="required",
        expr=check_required(F.col("names.primary")),
        shape=CheckShape.SCALAR,
        root_field="names",
    )


def _names_primary_string_min_length_check() -> Check:
    return Check(
        field="names.primary",
        name="string_min_length",
        expr=check_string_min_length(F.col("names.primary"), 1),
        shape=CheckShape.SCALAR,
        root_field="names",
    )


def _names_primary_stripped_check() -> Check:
    return Check(
        field="names.primary",
        name="stripped",
        expr=check_stripped(F.col("names.primary")),
        shape=CheckShape.SCALAR,
        root_field="names",
    )


def _names_rules_value_required_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="required",
        expr=array_check("names.rules", lambda el: check_required(el["value"])),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_value_string_min_length_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="string_min_length",
        expr=array_check(
            "names.rules", lambda el: check_string_min_length(el["value"], 1)
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_value_stripped_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="stripped",
        expr=array_check("names.rules", lambda el: check_stripped(el["value"])),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_variant_required_check() -> Check:
    return Check(
        field="names.rules[].variant",
        name="required",
        expr=array_check("names.rules", lambda el: check_required(el["variant"])),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_variant_enum_check() -> Check:
    return Check(
        field="names.rules[].variant",
        name="enum",
        expr=array_check(
            "names.rules",
            lambda el: check_enum(
                el["variant"], ["common", "official", "alternate", "short"]
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_language_check() -> Check:
    return Check(
        field="names.rules[].language",
        name="language_tag",
        expr=array_check(
            "names.rules",
            lambda el: check_pattern(
                el["language"],
                "^(?:(?:[A-Za-z]{2,3}(?:-[A-Za-z]{3}){0,3}?)|(?:[A-Za-z]{4,8}))(?:-[A-Za-z]{4})?(?:-[A-Za-z]{2}|[0-9]{3})?(?:-(?:[A-Za-z0-9]{5,8}|[0-9][A-Za-z0-9]{3}))*(?:-[A-WY-Za-wy-z0-9](?:-[A-Za-z0-9]{2,8})+)*\\z",
                label="IETF BCP-47 language tag",
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_mode_required_check() -> Check:
    return Check(
        field="names.rules[].perspectives.mode",
        name="required",
        expr=array_check(
            "names.rules",
            lambda el: F.when(
                el["perspectives"].isNotNull(),
                check_required(el["perspectives"]["mode"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_mode_enum_check() -> Check:
    return Check(
        field="names.rules[].perspectives.mode",
        name="enum",
        expr=array_check(
            "names.rules",
            lambda el: check_enum(
                el["perspectives"]["mode"], ["accepted_by", "disputed_by"]
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_countries_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries",
        name="required",
        expr=array_check(
            "names.rules",
            lambda el: F.when(
                el["perspectives"].isNotNull(),
                check_required(el["perspectives"]["countries"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_countries_min_length_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries_min_length",
        name="array_min_length",
        expr=array_check(
            "names.rules",
            lambda el: check_array_min_length(el["perspectives"]["countries"], 1),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_countries_unique_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries_unique",
        name="struct_unique",
        expr=array_check(
            "names.rules",
            lambda el: check_struct_unique(el["perspectives"]["countries"]),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_countries_check_1() -> Check:
    return Check(
        field="names.rules[].perspectives.countries[]",
        name="country_code_alpha2",
        expr=nested_array_check(
            "names.rules",
            lambda el: array_check(
                el["perspectives"]["countries"],
                lambda inner: check_pattern(
                    inner, "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_between_linear_range_length_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_length",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_between_linear_range_bounds_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_bounds",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_between_linear_range_order_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_order",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_order(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_side_check() -> Check:
    return Check(
        field="names.rules[].side",
        name="enum",
        expr=array_check(
            "names.rules", lambda el: check_enum(el["side"], ["left", "right"])
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _id_required_check() -> Check:
    return Check(
        field="id",
        name="required",
        expr=check_required(F.col("id")),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _id_string_min_length_check() -> Check:
    return Check(
        field="id",
        name="string_min_length",
        expr=check_string_min_length(F.col("id"), 1),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _id_no_whitespace_check() -> Check:
    return Check(
        field="id",
        name="no_whitespace",
        expr=check_pattern(
            F.col("id"), "^\\S+\\z", label="String without whitespace characters"
        ),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _bbox_bbox_completeness_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_completeness",
        expr=check_bbox_completeness(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _bbox_bbox_lat_ordering_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_lat_ordering",
        expr=check_bbox_lat_ordering(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _bbox_bbox_lat_range_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_lat_range",
        expr=check_bbox_lat_range(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _geometry_required_check() -> Check:
    return Check(
        field="geometry",
        name="required",
        expr=check_required(F.col("geometry")),
        shape=CheckShape.SCALAR,
        root_field="geometry",
    )


def _geometry_geometry_type_check() -> Check:
    return Check(
        field="geometry",
        name="geometry_type",
        expr=check_geometry_type(F.col("geometry"), GeometryType.POINT),
        shape=CheckShape.SCALAR,
        root_field="geometry",
    )


def _theme_required_check() -> Check:
    return Check(
        field="theme",
        name="required",
        expr=check_required(F.col("theme")),
        shape=CheckShape.SCALAR,
        root_field="theme",
    )


def _theme_enum_check() -> Check:
    return Check(
        field="theme",
        name="enum",
        expr=check_enum(F.col("theme"), ["divisions"]),
        shape=CheckShape.SCALAR,
        root_field="theme",
    )


def _type_required_check() -> Check:
    return Check(
        field="type",
        name="required",
        expr=check_required(F.col("type")),
        shape=CheckShape.SCALAR,
        root_field="type",
    )


def _type_enum_check() -> Check:
    return Check(
        field="type",
        name="enum",
        expr=check_enum(F.col("type"), ["division"]),
        shape=CheckShape.SCALAR,
        root_field="type",
    )


def _version_required_check() -> Check:
    return Check(
        field="version",
        name="required",
        expr=check_required(F.col("version")),
        shape=CheckShape.SCALAR,
        root_field="version",
    )


def _version_bounds_check() -> Check:
    return Check(
        field="version",
        name="bounds",
        expr=check_bounds(F.col("version"), ge=0),
        shape=CheckShape.SCALAR,
        root_field="version",
    )


def _sources_min_length_check() -> Check:
    return Check(
        field="sources_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("sources"), 1),
        shape=CheckShape.SCALAR,
        root_field="sources",
    )


def _sources_unique_check() -> Check:
    return Check(
        field="sources_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("sources")),
        shape=CheckShape.SCALAR,
        root_field="sources",
    )


def _sources_property_required_check() -> Check:
    return Check(
        field="sources[].property",
        name="required",
        expr=array_check("sources", lambda el: check_required(el["property"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_property_json_pointer_check() -> Check:
    return Check(
        field="sources[].property",
        name="json_pointer",
        expr=array_check("sources", lambda el: check_json_pointer(el["property"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_dataset_check() -> Check:
    return Check(
        field="sources[].dataset",
        name="required",
        expr=array_check("sources", lambda el: check_required(el["dataset"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_license_check() -> Check:
    return Check(
        field="sources[].license",
        name="stripped",
        expr=array_check("sources", lambda el: check_stripped(el["license"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_confidence_bounds_check() -> Check:
    return Check(
        field="sources[].confidence",
        name="bounds",
        expr=array_check("sources", lambda el: check_bounds(el["confidence"], ge=0.0)),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_confidence_bounds_check_1() -> Check:
    return Check(
        field="sources[].confidence",
        name="bounds",
        expr=array_check("sources", lambda el: check_bounds(el["confidence"], le=1.0)),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_length_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_length",
        expr=array_check(
            "sources", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_bounds_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_bounds",
        expr=array_check(
            "sources", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_order_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_order",
        expr=array_check("sources", lambda el: check_linear_range_order(el["between"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _subtype_required_check() -> Check:
    return Check(
        field="subtype",
        name="required",
        expr=check_required(F.col("subtype")),
        shape=CheckShape.SCALAR,
        root_field="subtype",
    )


def _subtype_enum_check() -> Check:
    return Check(
        field="subtype",
        name="enum",
        expr=check_enum(
            F.col("subtype"),
            [
                "country",
                "dependency",
                "macroregion",
                "region",
                "macrocounty",
                "county",
                "localadmin",
                "locality",
                "borough",
                "macrohood",
                "neighborhood",
                "microhood",
            ],
        ),
        shape=CheckShape.SCALAR,
        root_field="subtype",
    )


def _country_required_check() -> Check:
    return Check(
        field="country",
        name="required",
        expr=check_required(F.col("country")),
        shape=CheckShape.SCALAR,
        root_field="country",
    )


def _country_country_code_alpha2_check() -> Check:
    return Check(
        field="country",
        name="country_code_alpha2",
        expr=check_pattern(
            F.col("country"), "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
        ),
        shape=CheckShape.SCALAR,
        root_field="country",
    )


def _hierarchies_check() -> Check:
    return Check(
        field="hierarchies",
        name="required",
        expr=check_required(F.col("hierarchies")),
        shape=CheckShape.SCALAR,
        root_field="hierarchies",
    )


def _hierarchies_min_length_check() -> Check:
    return Check(
        field="hierarchies_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("hierarchies"), 1),
        shape=CheckShape.SCALAR,
        root_field="hierarchies",
    )


def _hierarchies_unique_check() -> Check:
    return Check(
        field="hierarchies_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("hierarchies")),
        shape=CheckShape.SCALAR,
        root_field="hierarchies",
    )


def _hierarchies_min_length_check_1() -> Check:
    return Check(
        field="hierarchies[]_min_length",
        name="array_min_length",
        expr=array_check("hierarchies", lambda el: check_array_min_length(el, 1)),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_unique_check_1() -> Check:
    return Check(
        field="hierarchies[]_unique",
        name="struct_unique",
        expr=array_check("hierarchies", lambda el: check_struct_unique(el)),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_division_id_required_check() -> Check:
    return Check(
        field="hierarchies[][].division_id",
        name="required",
        expr=nested_array_check(
            "hierarchies",
            lambda el: array_check(
                el, lambda inner: check_required(inner["division_id"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_division_id_string_min_length_check() -> Check:
    return Check(
        field="hierarchies[][].division_id",
        name="string_min_length",
        expr=nested_array_check(
            "hierarchies",
            lambda el: array_check(
                el, lambda inner: check_string_min_length(inner["division_id"], 1)
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_division_id_no_whitespace_check() -> Check:
    return Check(
        field="hierarchies[][].division_id",
        name="no_whitespace",
        expr=nested_array_check(
            "hierarchies",
            lambda el: array_check(
                el,
                lambda inner: check_pattern(
                    inner["division_id"],
                    "^\\S+\\z",
                    label="String without whitespace characters",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_subtype_required_check() -> Check:
    return Check(
        field="hierarchies[][].subtype",
        name="required",
        expr=nested_array_check(
            "hierarchies",
            lambda el: array_check(el, lambda inner: check_required(inner["subtype"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_subtype_enum_check() -> Check:
    return Check(
        field="hierarchies[][].subtype",
        name="enum",
        expr=nested_array_check(
            "hierarchies",
            lambda el: array_check(
                el,
                lambda inner: check_enum(
                    inner["subtype"],
                    [
                        "country",
                        "dependency",
                        "macroregion",
                        "region",
                        "macrocounty",
                        "county",
                        "localadmin",
                        "locality",
                        "borough",
                        "macrohood",
                        "neighborhood",
                        "microhood",
                    ],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_name_required_check() -> Check:
    return Check(
        field="hierarchies[][].name",
        name="required",
        expr=nested_array_check(
            "hierarchies",
            lambda el: array_check(el, lambda inner: check_required(inner["name"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_name_string_min_length_check() -> Check:
    return Check(
        field="hierarchies[][].name",
        name="string_min_length",
        expr=nested_array_check(
            "hierarchies",
            lambda el: array_check(
                el, lambda inner: check_string_min_length(inner["name"], 1)
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _hierarchies_name_stripped_check() -> Check:
    return Check(
        field="hierarchies[][].name",
        name="stripped",
        expr=nested_array_check(
            "hierarchies",
            lambda el: array_check(el, lambda inner: check_stripped(inner["name"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="hierarchies",
    )


def _parent_division_id_string_min_length_check() -> Check:
    return Check(
        field="parent_division_id",
        name="string_min_length",
        expr=check_string_min_length(F.col("parent_division_id"), 1),
        shape=CheckShape.SCALAR,
        root_field="parent_division_id",
    )


def _parent_division_id_no_whitespace_check() -> Check:
    return Check(
        field="parent_division_id",
        name="no_whitespace",
        expr=check_pattern(
            F.col("parent_division_id"),
            "^\\S+\\z",
            label="String without whitespace characters",
        ),
        shape=CheckShape.SCALAR,
        root_field="parent_division_id",
    )


def _admin_level_bounds_check() -> Check:
    return Check(
        field="admin_level",
        name="bounds",
        expr=check_bounds(F.col("admin_level"), ge=0),
        shape=CheckShape.SCALAR,
        root_field="admin_level",
    )


def _admin_level_bounds_check_1() -> Check:
    return Check(
        field="admin_level",
        name="bounds",
        expr=check_bounds(F.col("admin_level"), le=16),
        shape=CheckShape.SCALAR,
        root_field="admin_level",
    )


def _class_check() -> Check:
    return Check(
        field="class",
        name="enum",
        expr=check_enum(
            F.col("class"), ["megacity", "city", "town", "village", "hamlet"]
        ),
        shape=CheckShape.SCALAR,
        root_field="class",
    )


def _region_check() -> Check:
    return Check(
        field="region",
        name="region_code",
        expr=check_pattern(
            F.col("region"),
            "^[A-Z]{2}-[A-Z0-9]{1,3}\\z",
            label="ISO 3166-2 subdivision code",
        ),
        shape=CheckShape.SCALAR,
        root_field="region",
    )


def _perspectives_mode_required_check() -> Check:
    return Check(
        field="perspectives.mode",
        name="required",
        expr=F.when(
            F.col("perspectives").isNotNull(),
            check_required(F.col("perspectives.mode")),
        ),
        shape=CheckShape.SCALAR,
        root_field="perspectives",
    )


def _perspectives_mode_enum_check() -> Check:
    return Check(
        field="perspectives.mode",
        name="enum",
        expr=check_enum(F.col("perspectives.mode"), ["accepted_by", "disputed_by"]),
        shape=CheckShape.SCALAR,
        root_field="perspectives",
    )


def _perspectives_countries_check() -> Check:
    return Check(
        field="perspectives.countries",
        name="required",
        expr=F.when(
            F.col("perspectives").isNotNull(),
            check_required(F.col("perspectives.countries")),
        ),
        shape=CheckShape.SCALAR,
        root_field="perspectives",
    )


def _perspectives_countries_min_length_check() -> Check:
    return Check(
        field="perspectives.countries_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("perspectives.countries"), 1),
        shape=CheckShape.SCALAR,
        root_field="perspectives",
    )


def _perspectives_countries_unique_check() -> Check:
    return Check(
        field="perspectives.countries_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("perspectives.countries")),
        shape=CheckShape.SCALAR,
        root_field="perspectives",
    )


def _perspectives_countries_check_1() -> Check:
    return Check(
        field="perspectives.countries[]",
        name="country_code_alpha2",
        expr=array_check(
            "perspectives.countries",
            lambda el: check_pattern(
                el, "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="perspectives",
    )


def _norms_driving_side_check() -> Check:
    return Check(
        field="norms.driving_side",
        name="enum",
        expr=check_enum(F.col("norms.driving_side"), ["left", "right"]),
        shape=CheckShape.SCALAR,
        root_field="norms",
    )


def _population_check() -> Check:
    return Check(
        field="population",
        name="bounds",
        expr=check_bounds(F.col("population"), ge=0),
        shape=CheckShape.SCALAR,
        root_field="population",
    )


def _capital_division_ids_min_length_check() -> Check:
    return Check(
        field="capital_division_ids_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("capital_division_ids"), 1),
        shape=CheckShape.SCALAR,
        root_field="capital_division_ids",
    )


def _capital_division_ids_unique_check() -> Check:
    return Check(
        field="capital_division_ids_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("capital_division_ids")),
        shape=CheckShape.SCALAR,
        root_field="capital_division_ids",
    )


def _capital_division_ids_string_min_length_check() -> Check:
    return Check(
        field="capital_division_ids[]",
        name="string_min_length",
        expr=array_check(
            "capital_division_ids", lambda el: check_string_min_length(el, 1)
        ),
        shape=CheckShape.ARRAY,
        root_field="capital_division_ids",
    )


def _capital_division_ids_no_whitespace_check() -> Check:
    return Check(
        field="capital_division_ids[]",
        name="no_whitespace",
        expr=array_check(
            "capital_division_ids",
            lambda el: check_pattern(
                el, "^\\S+\\z", label="String without whitespace characters"
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="capital_division_ids",
    )


def _capital_of_divisions_min_length_check() -> Check:
    return Check(
        field="capital_of_divisions_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("capital_of_divisions"), 1),
        shape=CheckShape.SCALAR,
        root_field="capital_of_divisions",
    )


def _capital_of_divisions_unique_check() -> Check:
    return Check(
        field="capital_of_divisions_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("capital_of_divisions")),
        shape=CheckShape.SCALAR,
        root_field="capital_of_divisions",
    )


def _capital_of_divisions_division_id_required_check() -> Check:
    return Check(
        field="capital_of_divisions[].division_id",
        name="required",
        expr=array_check(
            "capital_of_divisions", lambda el: check_required(el["division_id"])
        ),
        shape=CheckShape.ARRAY,
        root_field="capital_of_divisions",
    )


def _capital_of_divisions_division_id_string_min_length_check() -> Check:
    return Check(
        field="capital_of_divisions[].division_id",
        name="string_min_length",
        expr=array_check(
            "capital_of_divisions",
            lambda el: check_string_min_length(el["division_id"], 1),
        ),
        shape=CheckShape.ARRAY,
        root_field="capital_of_divisions",
    )


def _capital_of_divisions_division_id_no_whitespace_check() -> Check:
    return Check(
        field="capital_of_divisions[].division_id",
        name="no_whitespace",
        expr=array_check(
            "capital_of_divisions",
            lambda el: check_pattern(
                el["division_id"],
                "^\\S+\\z",
                label="String without whitespace characters",
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="capital_of_divisions",
    )


def _capital_of_divisions_subtype_required_check() -> Check:
    return Check(
        field="capital_of_divisions[].subtype",
        name="required",
        expr=array_check(
            "capital_of_divisions", lambda el: check_required(el["subtype"])
        ),
        shape=CheckShape.ARRAY,
        root_field="capital_of_divisions",
    )


def _capital_of_divisions_subtype_enum_check() -> Check:
    return Check(
        field="capital_of_divisions[].subtype",
        name="enum",
        expr=array_check(
            "capital_of_divisions",
            lambda el: check_enum(
                el["subtype"],
                [
                    "country",
                    "dependency",
                    "macroregion",
                    "region",
                    "macrocounty",
                    "county",
                    "localadmin",
                    "locality",
                    "borough",
                    "macrohood",
                    "neighborhood",
                    "microhood",
                ],
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="capital_of_divisions",
    )


def _wikidata_check() -> Check:
    return Check(
        field="wikidata",
        name="wikidata_id",
        expr=check_pattern(
            F.col("wikidata"),
            "^Q\\d+\\z",
            label="Wikidata identifier (Q followed by digits)",
        ),
        shape=CheckShape.SCALAR,
        root_field="wikidata",
    )


def _check_require_if_0_check() -> Check:
    return Check(
        field="admin_level_required_0",
        name="require_if",
        expr=check_require_if(
            F.col("admin_level"), F.col("subtype") == "county", "subtype = 'county'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_require_if_1_check() -> Check:
    return Check(
        field="admin_level_required_1",
        name="require_if",
        expr=check_require_if(
            F.col("admin_level"),
            F.col("subtype") == "macrocounty",
            "subtype = 'macrocounty'",
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_require_if_2_check() -> Check:
    return Check(
        field="admin_level_required_2",
        name="require_if",
        expr=check_require_if(
            F.col("admin_level"), F.col("subtype") == "region", "subtype = 'region'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_require_if_3_check() -> Check:
    return Check(
        field="admin_level_required_3",
        name="require_if",
        expr=check_require_if(
            F.col("admin_level"),
            F.col("subtype") == "macroregion",
            "subtype = 'macroregion'",
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_require_if_4_check() -> Check:
    return Check(
        field="admin_level_required_4",
        name="require_if",
        expr=check_require_if(
            F.col("admin_level"),
            F.col("subtype") == "dependency",
            "subtype = 'dependency'",
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_require_if_5_check() -> Check:
    return Check(
        field="admin_level_required_5",
        name="require_if",
        expr=check_require_if(
            F.col("admin_level"), F.col("subtype") == "country", "subtype = 'country'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_require_if_6_check() -> Check:
    return Check(
        field="parent_division_id_required",
        name="require_if",
        expr=check_require_if(
            F.col("parent_division_id"),
            F.col("subtype") != "country",
            "subtype != 'country'",
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_7_check() -> Check:
    return Check(
        field="parent_division_id_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("parent_division_id"),
            F.col("subtype") == "country",
            "subtype = 'country'",
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def division_checks() -> list[Check]:
    """All validation checks for division."""
    return [
        _cartography_prominence_bounds_check(),
        _cartography_prominence_bounds_check_1(),
        _cartography_min_zoom_bounds_check(),
        _cartography_min_zoom_bounds_check_1(),
        _cartography_max_zoom_bounds_check(),
        _cartography_max_zoom_bounds_check_1(),
        _names_check(),
        _names_primary_required_check(),
        _names_primary_string_min_length_check(),
        _names_primary_stripped_check(),
        _names_rules_value_required_check(),
        _names_rules_value_string_min_length_check(),
        _names_rules_value_stripped_check(),
        _names_rules_variant_required_check(),
        _names_rules_variant_enum_check(),
        _names_rules_language_check(),
        _names_rules_perspectives_mode_required_check(),
        _names_rules_perspectives_mode_enum_check(),
        _names_rules_perspectives_countries_check(),
        _names_rules_perspectives_countries_min_length_check(),
        _names_rules_perspectives_countries_unique_check(),
        _names_rules_perspectives_countries_check_1(),
        _names_rules_between_linear_range_length_check(),
        _names_rules_between_linear_range_bounds_check(),
        _names_rules_between_linear_range_order_check(),
        _names_rules_side_check(),
        _id_required_check(),
        _id_string_min_length_check(),
        _id_no_whitespace_check(),
        _bbox_bbox_completeness_check(),
        _bbox_bbox_lat_ordering_check(),
        _bbox_bbox_lat_range_check(),
        _geometry_required_check(),
        _geometry_geometry_type_check(),
        _theme_required_check(),
        _theme_enum_check(),
        _type_required_check(),
        _type_enum_check(),
        _version_required_check(),
        _version_bounds_check(),
        _sources_min_length_check(),
        _sources_unique_check(),
        _sources_property_required_check(),
        _sources_property_json_pointer_check(),
        _sources_dataset_check(),
        _sources_license_check(),
        _sources_confidence_bounds_check(),
        _sources_confidence_bounds_check_1(),
        _sources_between_linear_range_length_check(),
        _sources_between_linear_range_bounds_check(),
        _sources_between_linear_range_order_check(),
        _subtype_required_check(),
        _subtype_enum_check(),
        _country_required_check(),
        _country_country_code_alpha2_check(),
        _hierarchies_check(),
        _hierarchies_min_length_check(),
        _hierarchies_unique_check(),
        _hierarchies_min_length_check_1(),
        _hierarchies_unique_check_1(),
        _hierarchies_division_id_required_check(),
        _hierarchies_division_id_string_min_length_check(),
        _hierarchies_division_id_no_whitespace_check(),
        _hierarchies_subtype_required_check(),
        _hierarchies_subtype_enum_check(),
        _hierarchies_name_required_check(),
        _hierarchies_name_string_min_length_check(),
        _hierarchies_name_stripped_check(),
        _parent_division_id_string_min_length_check(),
        _parent_division_id_no_whitespace_check(),
        _admin_level_bounds_check(),
        _admin_level_bounds_check_1(),
        _class_check(),
        _region_check(),
        _perspectives_mode_required_check(),
        _perspectives_mode_enum_check(),
        _perspectives_countries_check(),
        _perspectives_countries_min_length_check(),
        _perspectives_countries_unique_check(),
        _perspectives_countries_check_1(),
        _norms_driving_side_check(),
        _population_check(),
        _capital_division_ids_min_length_check(),
        _capital_division_ids_unique_check(),
        _capital_division_ids_string_min_length_check(),
        _capital_division_ids_no_whitespace_check(),
        _capital_of_divisions_min_length_check(),
        _capital_of_divisions_unique_check(),
        _capital_of_divisions_division_id_required_check(),
        _capital_of_divisions_division_id_string_min_length_check(),
        _capital_of_divisions_division_id_no_whitespace_check(),
        _capital_of_divisions_subtype_required_check(),
        _capital_of_divisions_subtype_enum_check(),
        _wikidata_check(),
        _check_require_if_0_check(),
        _check_require_if_1_check(),
        _check_require_if_2_check(),
        _check_require_if_3_check(),
        _check_require_if_4_check(),
        _check_require_if_5_check(),
        _check_require_if_6_check(),
        _check_forbid_if_7_check(),
    ]


DIVISION_SCHEMA = StructType(
    [
        StructField(
            "cartography",
            StructType(
                [
                    StructField("prominence", IntegerType(), True),
                    StructField("min_zoom", IntegerType(), True),
                    StructField("max_zoom", IntegerType(), True),
                    StructField("sort_key", IntegerType(), True),
                ]
            ),
            True,
        ),
        StructField(
            "names",
            StructType(
                [
                    StructField("primary", StringType(), True),
                    StructField(
                        "common", MapType(StringType(), StringType(), True), True
                    ),
                    StructField(
                        "rules",
                        ArrayType(
                            StructType(
                                [
                                    StructField("value", StringType(), True),
                                    StructField("variant", StringType(), True),
                                    StructField("language", StringType(), True),
                                    StructField(
                                        "perspectives",
                                        StructType(
                                            [
                                                StructField("mode", StringType(), True),
                                                StructField(
                                                    "countries",
                                                    ArrayType(StringType(), True),
                                                    True,
                                                ),
                                            ]
                                        ),
                                        True,
                                    ),
                                    StructField(
                                        "between", ArrayType(DoubleType(), True), True
                                    ),
                                    StructField("side", StringType(), True),
                                ]
                            ),
                            True,
                        ),
                        True,
                    ),
                ]
            ),
            True,
        ),
        StructField("id", StringType(), True),
        StructField("bbox", BBOX_STRUCT, True),
        StructField("geometry", BinaryType(), True),
        StructField("theme", StringType(), True),
        StructField("type", StringType(), True),
        StructField("version", IntegerType(), True),
        StructField(
            "sources",
            ArrayType(
                StructType(
                    [
                        StructField("property", StringType(), True),
                        StructField("dataset", StringType(), True),
                        StructField("license", StringType(), True),
                        StructField("record_id", StringType(), True),
                        StructField("update_time", StringType(), True),
                        StructField("confidence", DoubleType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField("subtype", StringType(), True),
        StructField("country", StringType(), True),
        StructField(
            "hierarchies",
            ArrayType(
                ArrayType(
                    StructType(
                        [
                            StructField("division_id", StringType(), True),
                            StructField("subtype", StringType(), True),
                            StructField("name", StringType(), True),
                        ]
                    ),
                    True,
                ),
                True,
            ),
            True,
        ),
        StructField("parent_division_id", StringType(), True),
        StructField("admin_level", IntegerType(), True),
        StructField("class", StringType(), True),
        StructField("local_type", MapType(StringType(), StringType(), True), True),
        StructField("region", StringType(), True),
        StructField(
            "perspectives",
            StructType(
                [
                    StructField("mode", StringType(), True),
                    StructField("countries", ArrayType(StringType(), True), True),
                ]
            ),
            True,
        ),
        StructField(
            "norms", StructType([StructField("driving_side", StringType(), True)]), True
        ),
        StructField("population", IntegerType(), True),
        StructField("capital_division_ids", ArrayType(StringType(), True), True),
        StructField(
            "capital_of_divisions",
            ArrayType(
                StructType(
                    [
                        StructField("division_id", StringType(), True),
                        StructField("subtype", StringType(), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField("wikidata", StringType(), True),
    ]
)

GEOMETRY_TYPES: tuple[GeometryType, ...] = (GeometryType.POINT,)

ENTRY_POINT = "overture.schema.divisions:Division"

PARTITIONS: dict[str, str] = {"theme": "divisions"}

FEATURE_VALIDATION = FeatureValidation(
    schema=DIVISION_SCHEMA,
    checks=division_checks,
    geometry_types=GEOMETRY_TYPES,
)
