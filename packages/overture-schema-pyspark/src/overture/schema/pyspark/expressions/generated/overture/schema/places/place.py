# This file is auto-generated by overture-schema-codegen. Do not edit.

"""Place validation expression builders."""

from __future__ import annotations

from pyspark.sql import functions as F
from pyspark.sql.types import (
    ArrayType,
    BinaryType,
    DoubleType,
    IntegerType,
    MapType,
    StringType,
    StructField,
    StructType,
)

from overture.schema.pyspark.check import Check, CheckShape, ModelValidation
from overture.schema.pyspark.expressions._schema_structs import (
    BBOX_STRUCT,
)
from overture.schema.pyspark.expressions.column_patterns import (
    array_check,
    check_struct_unique,
    map_keys_check,
    map_values_check,
    nested_array_check,
)
from overture.schema.pyspark.expressions.constraint_expressions import (
    check_array_min_length,
    check_bbox_completeness,
    check_bbox_lat_ordering,
    check_bbox_lat_range,
    check_bounds,
    check_email,
    check_enum,
    check_geometry_type,
    check_json_pointer,
    check_linear_range_bounds,
    check_linear_range_length,
    check_linear_range_order,
    check_pattern,
    check_required,
    check_string_min_length,
    check_stripped,
    check_url_format,
    check_url_length,
)
from overture.schema.system.primitive import GeometryType


def _id_required_check() -> Check:
    return Check(
        field="id",
        name="required",
        expr=check_required(F.col("id")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"id"}),
    )


def _id_string_min_length_check() -> Check:
    return Check(
        field="id",
        name="string_min_length",
        expr=check_string_min_length(F.col("id"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"id"}),
    )


def _id_no_whitespace_check() -> Check:
    return Check(
        field="id",
        name="no_whitespace",
        expr=check_pattern(
            F.col("id"), "^\\S+\\z", label="String without whitespace characters"
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"id"}),
    )


def _bbox_bbox_completeness_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_completeness",
        expr=check_bbox_completeness(F.col("bbox")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"bbox"}),
    )


def _bbox_bbox_lat_ordering_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_lat_ordering",
        expr=check_bbox_lat_ordering(F.col("bbox")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"bbox"}),
    )


def _bbox_bbox_lat_range_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_lat_range",
        expr=check_bbox_lat_range(F.col("bbox")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"bbox"}),
    )


def _geometry_required_check() -> Check:
    return Check(
        field="geometry",
        name="required",
        expr=check_required(F.col("geometry")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"geometry"}),
    )


def _geometry_geometry_type_check() -> Check:
    return Check(
        field="geometry",
        name="geometry_type",
        expr=check_geometry_type(F.col("geometry"), GeometryType.POINT),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"geometry"}),
    )


def _theme_required_check() -> Check:
    return Check(
        field="theme",
        name="required",
        expr=check_required(F.col("theme")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"theme"}),
    )


def _theme_enum_check() -> Check:
    return Check(
        field="theme",
        name="enum",
        expr=check_enum(F.col("theme"), ["places"]),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"theme"}),
    )


def _type_required_check() -> Check:
    return Check(
        field="type",
        name="required",
        expr=check_required(F.col("type")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"type"}),
    )


def _type_enum_check() -> Check:
    return Check(
        field="type",
        name="enum",
        expr=check_enum(F.col("type"), ["place"]),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"type"}),
    )


def _version_required_check() -> Check:
    return Check(
        field="version",
        name="required",
        expr=check_required(F.col("version")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"version"}),
    )


def _version_bounds_check() -> Check:
    return Check(
        field="version",
        name="bounds",
        expr=check_bounds(F.col("version"), ge=0, check_nan=False),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"version"}),
    )


def _sources_min_length_check() -> Check:
    return Check(
        field="sources_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("sources"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"sources"}),
    )


def _sources_unique_check() -> Check:
    return Check(
        field="sources_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("sources")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"sources"}),
    )


def _sources_property_required_check() -> Check:
    return Check(
        field="sources[].property",
        name="required",
        expr=array_check("sources", lambda el: check_required(el["property"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _sources_property_json_pointer_check() -> Check:
    return Check(
        field="sources[].property",
        name="json_pointer",
        expr=array_check("sources", lambda el: check_json_pointer(el["property"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _sources_dataset_check() -> Check:
    return Check(
        field="sources[].dataset",
        name="required",
        expr=array_check("sources", lambda el: check_required(el["dataset"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _sources_license_check() -> Check:
    return Check(
        field="sources[].license",
        name="stripped",
        expr=array_check("sources", lambda el: check_stripped(el["license"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _sources_confidence_bounds_check() -> Check:
    return Check(
        field="sources[].confidence_0",
        name="bounds",
        expr=array_check("sources", lambda el: check_bounds(el["confidence"], ge=0.0)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _sources_confidence_bounds_check_1() -> Check:
    return Check(
        field="sources[].confidence_1",
        name="bounds",
        expr=array_check("sources", lambda el: check_bounds(el["confidence"], le=1.0)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _sources_between_linear_range_length_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_length",
        expr=array_check(
            "sources", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _sources_between_linear_range_bounds_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_bounds",
        expr=array_check(
            "sources", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _sources_between_linear_range_order_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_order",
        expr=array_check("sources", lambda el: check_linear_range_order(el["between"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"sources"}),
    )


def _operating_status_check() -> Check:
    return Check(
        field="operating_status",
        name="enum",
        expr=check_enum(
            F.col("operating_status"),
            ["open", "permanently_closed", "temporarily_closed"],
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"operating_status"}),
    )


def _categories_primary_required_check() -> Check:
    return Check(
        field="categories.primary",
        name="required",
        expr=F.when(
            F.col("categories").isNotNull(), check_required(F.col("categories.primary"))
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"categories"}),
    )


def _categories_primary_snake_case_check() -> Check:
    return Check(
        field="categories.primary",
        name="snake_case",
        expr=check_pattern(
            F.col("categories.primary"),
            "^[a-z0-9]+(_[a-z0-9]+)*\\z",
            label="Category in snake_case format",
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"categories"}),
    )


def _categories_alternate_unique_check() -> Check:
    return Check(
        field="categories.alternate_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("categories.alternate")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"categories"}),
    )


def _categories_alternate_check() -> Check:
    return Check(
        field="categories.alternate[]",
        name="snake_case",
        expr=array_check(
            "categories.alternate",
            lambda el: check_pattern(
                el, "^[a-z0-9]+(_[a-z0-9]+)*\\z", label="Category in snake_case format"
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"categories"}),
    )


def _basic_category_check() -> Check:
    return Check(
        field="basic_category",
        name="snake_case",
        expr=check_pattern(
            F.col("basic_category"),
            "^[a-z0-9]+(_[a-z0-9]+)*\\z",
            label="Category in snake_case format",
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"basic_category"}),
    )


def _taxonomy_primary_required_check() -> Check:
    return Check(
        field="taxonomy.primary",
        name="required",
        expr=F.when(
            F.col("taxonomy").isNotNull(), check_required(F.col("taxonomy.primary"))
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"taxonomy"}),
    )


def _taxonomy_primary_snake_case_check() -> Check:
    return Check(
        field="taxonomy.primary",
        name="snake_case",
        expr=check_pattern(
            F.col("taxonomy.primary"),
            "^[a-z0-9]+(_[a-z0-9]+)*\\z",
            label="Category in snake_case format",
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"taxonomy"}),
    )


def _taxonomy_hierarchy_check() -> Check:
    return Check(
        field="taxonomy.hierarchy",
        name="required",
        expr=F.when(
            F.col("taxonomy").isNotNull(), check_required(F.col("taxonomy.hierarchy"))
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"taxonomy"}),
    )


def _taxonomy_hierarchy_min_length_check() -> Check:
    return Check(
        field="taxonomy.hierarchy_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("taxonomy.hierarchy"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"taxonomy"}),
    )


def _taxonomy_hierarchy_unique_check() -> Check:
    return Check(
        field="taxonomy.hierarchy_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("taxonomy.hierarchy")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"taxonomy"}),
    )


def _taxonomy_hierarchy_check_1() -> Check:
    return Check(
        field="taxonomy.hierarchy[]",
        name="snake_case",
        expr=array_check(
            "taxonomy.hierarchy",
            lambda el: check_pattern(
                el, "^[a-z0-9]+(_[a-z0-9]+)*\\z", label="Category in snake_case format"
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"taxonomy"}),
    )


def _taxonomy_alternates_min_length_check() -> Check:
    return Check(
        field="taxonomy.alternates_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("taxonomy.alternates"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"taxonomy"}),
    )


def _taxonomy_alternates_unique_check() -> Check:
    return Check(
        field="taxonomy.alternates_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("taxonomy.alternates")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"taxonomy"}),
    )


def _taxonomy_alternates_check() -> Check:
    return Check(
        field="taxonomy.alternates[]",
        name="snake_case",
        expr=array_check(
            "taxonomy.alternates",
            lambda el: check_pattern(
                el, "^[a-z0-9]+(_[a-z0-9]+)*\\z", label="Category in snake_case format"
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"taxonomy"}),
    )


def _confidence_bounds_check() -> Check:
    return Check(
        field="confidence_0",
        name="bounds",
        expr=check_bounds(F.col("confidence"), ge=0.0),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"confidence"}),
    )


def _confidence_bounds_check_1() -> Check:
    return Check(
        field="confidence_1",
        name="bounds",
        expr=check_bounds(F.col("confidence"), le=1.0),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"confidence"}),
    )


def _websites_min_length_check() -> Check:
    return Check(
        field="websites_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("websites"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"websites"}),
    )


def _websites_unique_check() -> Check:
    return Check(
        field="websites_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("websites")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"websites"}),
    )


def _websites_url_format_check() -> Check:
    return Check(
        field="websites[]",
        name="url_format",
        expr=array_check("websites", lambda el: check_url_format(el)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"websites"}),
    )


def _websites_url_length_check() -> Check:
    return Check(
        field="websites[]",
        name="url_length",
        expr=array_check("websites", lambda el: check_url_length(el)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"websites"}),
    )


def _socials_min_length_check() -> Check:
    return Check(
        field="socials_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("socials"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"socials"}),
    )


def _socials_unique_check() -> Check:
    return Check(
        field="socials_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("socials")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"socials"}),
    )


def _socials_url_format_check() -> Check:
    return Check(
        field="socials[]",
        name="url_format",
        expr=array_check("socials", lambda el: check_url_format(el)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"socials"}),
    )


def _socials_url_length_check() -> Check:
    return Check(
        field="socials[]",
        name="url_length",
        expr=array_check("socials", lambda el: check_url_length(el)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"socials"}),
    )


def _emails_min_length_check() -> Check:
    return Check(
        field="emails_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("emails"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"emails"}),
    )


def _emails_unique_check() -> Check:
    return Check(
        field="emails_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("emails")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"emails"}),
    )


def _emails_check() -> Check:
    return Check(
        field="emails[]",
        name="email",
        expr=array_check("emails", lambda el: check_email(el)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"emails"}),
    )


def _phones_min_length_check() -> Check:
    return Check(
        field="phones_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("phones"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"phones"}),
    )


def _phones_unique_check() -> Check:
    return Check(
        field="phones_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("phones")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"phones"}),
    )


def _phones_check() -> Check:
    return Check(
        field="phones[]",
        name="phone_number",
        expr=array_check(
            "phones",
            lambda el: check_pattern(
                el,
                "^\\+\\d{1,3}[\\s\\-\\(\\)0-9]+\\z",
                label="International phone number (+ followed by country code and number)",
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"phones"}),
    )


def _brand_names_primary_required_check() -> Check:
    return Check(
        field="brand.names.primary",
        name="required",
        expr=F.when(
            F.col("brand.names").isNotNull(),
            check_required(F.col("brand.names.primary")),
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_primary_string_min_length_check() -> Check:
    return Check(
        field="brand.names.primary",
        name="string_min_length",
        expr=check_string_min_length(F.col("brand.names.primary"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_primary_stripped_check() -> Check:
    return Check(
        field="brand.names.primary",
        name="stripped",
        expr=check_stripped(F.col("brand.names.primary")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_common_key_check() -> Check:
    return Check(
        field="brand.names.common{key}",
        name="language_tag",
        expr=map_keys_check(
            "brand.names.common",
            lambda k: check_pattern(
                k,
                "^(?:(?:[A-Za-z]{2,3}(?:-[A-Za-z]{3}){0,3}?)|(?:[A-Za-z]{4,8}))(?:-[A-Za-z]{4})?(?:-[A-Za-z]{2}|[0-9]{3})?(?:-(?:[A-Za-z0-9]{5,8}|[0-9][A-Za-z0-9]{3}))*(?:-[A-WY-Za-wy-z0-9](?:-[A-Za-z0-9]{2,8})+)*\\z",
                label="IETF BCP-47 language tag",
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_common_value_check() -> Check:
    return Check(
        field="brand.names.common{value}",
        name="stripped",
        expr=map_values_check("brand.names.common", lambda v: check_stripped(v)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_value_required_check() -> Check:
    return Check(
        field="brand.names.rules[].value",
        name="required",
        expr=array_check("brand.names.rules", lambda el: check_required(el["value"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_value_string_min_length_check() -> Check:
    return Check(
        field="brand.names.rules[].value",
        name="string_min_length",
        expr=array_check(
            "brand.names.rules", lambda el: check_string_min_length(el["value"], 1)
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_value_stripped_check() -> Check:
    return Check(
        field="brand.names.rules[].value",
        name="stripped",
        expr=array_check("brand.names.rules", lambda el: check_stripped(el["value"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_variant_required_check() -> Check:
    return Check(
        field="brand.names.rules[].variant",
        name="required",
        expr=array_check("brand.names.rules", lambda el: check_required(el["variant"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_variant_enum_check() -> Check:
    return Check(
        field="brand.names.rules[].variant",
        name="enum",
        expr=array_check(
            "brand.names.rules",
            lambda el: check_enum(
                el["variant"], ["common", "official", "alternate", "short"]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_language_check() -> Check:
    return Check(
        field="brand.names.rules[].language",
        name="language_tag",
        expr=array_check(
            "brand.names.rules",
            lambda el: check_pattern(
                el["language"],
                "^(?:(?:[A-Za-z]{2,3}(?:-[A-Za-z]{3}){0,3}?)|(?:[A-Za-z]{4,8}))(?:-[A-Za-z]{4})?(?:-[A-Za-z]{2}|[0-9]{3})?(?:-(?:[A-Za-z0-9]{5,8}|[0-9][A-Za-z0-9]{3}))*(?:-[A-WY-Za-wy-z0-9](?:-[A-Za-z0-9]{2,8})+)*\\z",
                label="IETF BCP-47 language tag",
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_perspectives_mode_required_check() -> Check:
    return Check(
        field="brand.names.rules[].perspectives.mode",
        name="required",
        expr=array_check(
            "brand.names.rules",
            lambda el: F.when(
                el["perspectives"].isNotNull(),
                check_required(el["perspectives"]["mode"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_perspectives_mode_enum_check() -> Check:
    return Check(
        field="brand.names.rules[].perspectives.mode",
        name="enum",
        expr=array_check(
            "brand.names.rules",
            lambda el: check_enum(
                el["perspectives"]["mode"], ["accepted_by", "disputed_by"]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_perspectives_countries_check() -> Check:
    return Check(
        field="brand.names.rules[].perspectives.countries",
        name="required",
        expr=array_check(
            "brand.names.rules",
            lambda el: F.when(
                el["perspectives"].isNotNull(),
                check_required(el["perspectives"]["countries"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_perspectives_countries_min_length_check() -> Check:
    return Check(
        field="brand.names.rules[].perspectives.countries_min_length",
        name="array_min_length",
        expr=array_check(
            "brand.names.rules",
            lambda el: check_array_min_length(el["perspectives"]["countries"], 1),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_perspectives_countries_unique_check() -> Check:
    return Check(
        field="brand.names.rules[].perspectives.countries_unique",
        name="struct_unique",
        expr=array_check(
            "brand.names.rules",
            lambda el: check_struct_unique(el["perspectives"]["countries"]),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_perspectives_countries_check_1() -> Check:
    return Check(
        field="brand.names.rules[].perspectives.countries[]",
        name="country_code_alpha2",
        expr=nested_array_check(
            "brand.names.rules",
            lambda el: array_check(
                el["perspectives"]["countries"],
                lambda inner: check_pattern(
                    inner, "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_between_linear_range_length_check() -> Check:
    return Check(
        field="brand.names.rules[].between",
        name="linear_range_length",
        expr=array_check(
            "brand.names.rules", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_between_linear_range_bounds_check() -> Check:
    return Check(
        field="brand.names.rules[].between",
        name="linear_range_bounds",
        expr=array_check(
            "brand.names.rules", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_between_linear_range_order_check() -> Check:
    return Check(
        field="brand.names.rules[].between",
        name="linear_range_order",
        expr=array_check(
            "brand.names.rules", lambda el: check_linear_range_order(el["between"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_names_rules_side_check() -> Check:
    return Check(
        field="brand.names.rules[].side",
        name="enum",
        expr=array_check(
            "brand.names.rules", lambda el: check_enum(el["side"], ["left", "right"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"brand"}),
    )


def _brand_wikidata_check() -> Check:
    return Check(
        field="brand.wikidata",
        name="wikidata_id",
        expr=check_pattern(
            F.col("brand.wikidata"),
            "^Q\\d+\\z",
            label="Wikidata identifier (Q followed by digits)",
        ),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"brand"}),
    )


def _addresses_min_length_check() -> Check:
    return Check(
        field="addresses_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("addresses"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"addresses"}),
    )


def _addresses_region_check() -> Check:
    return Check(
        field="addresses[].region",
        name="region_code",
        expr=array_check(
            "addresses",
            lambda el: check_pattern(
                el["region"],
                "^[A-Z]{2}-[A-Z0-9]{1,3}\\z",
                label="ISO 3166-2 subdivision code",
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"addresses"}),
    )


def _addresses_country_check() -> Check:
    return Check(
        field="addresses[].country",
        name="country_code_alpha2",
        expr=array_check(
            "addresses",
            lambda el: check_pattern(
                el["country"], "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"addresses"}),
    )


def _names_primary_required_check() -> Check:
    return Check(
        field="names.primary",
        name="required",
        expr=F.when(F.col("names").isNotNull(), check_required(F.col("names.primary"))),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"names"}),
    )


def _names_primary_string_min_length_check() -> Check:
    return Check(
        field="names.primary",
        name="string_min_length",
        expr=check_string_min_length(F.col("names.primary"), 1),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"names"}),
    )


def _names_primary_stripped_check() -> Check:
    return Check(
        field="names.primary",
        name="stripped",
        expr=check_stripped(F.col("names.primary")),
        shape=CheckShape.SCALAR,
        read_columns=frozenset({"names"}),
    )


def _names_common_key_check() -> Check:
    return Check(
        field="names.common{key}",
        name="language_tag",
        expr=map_keys_check(
            "names.common",
            lambda k: check_pattern(
                k,
                "^(?:(?:[A-Za-z]{2,3}(?:-[A-Za-z]{3}){0,3}?)|(?:[A-Za-z]{4,8}))(?:-[A-Za-z]{4})?(?:-[A-Za-z]{2}|[0-9]{3})?(?:-(?:[A-Za-z0-9]{5,8}|[0-9][A-Za-z0-9]{3}))*(?:-[A-WY-Za-wy-z0-9](?:-[A-Za-z0-9]{2,8})+)*\\z",
                label="IETF BCP-47 language tag",
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_common_value_check() -> Check:
    return Check(
        field="names.common{value}",
        name="stripped",
        expr=map_values_check("names.common", lambda v: check_stripped(v)),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_value_required_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="required",
        expr=array_check("names.rules", lambda el: check_required(el["value"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_value_string_min_length_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="string_min_length",
        expr=array_check(
            "names.rules", lambda el: check_string_min_length(el["value"], 1)
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_value_stripped_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="stripped",
        expr=array_check("names.rules", lambda el: check_stripped(el["value"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_variant_required_check() -> Check:
    return Check(
        field="names.rules[].variant",
        name="required",
        expr=array_check("names.rules", lambda el: check_required(el["variant"])),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_variant_enum_check() -> Check:
    return Check(
        field="names.rules[].variant",
        name="enum",
        expr=array_check(
            "names.rules",
            lambda el: check_enum(
                el["variant"], ["common", "official", "alternate", "short"]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_language_check() -> Check:
    return Check(
        field="names.rules[].language",
        name="language_tag",
        expr=array_check(
            "names.rules",
            lambda el: check_pattern(
                el["language"],
                "^(?:(?:[A-Za-z]{2,3}(?:-[A-Za-z]{3}){0,3}?)|(?:[A-Za-z]{4,8}))(?:-[A-Za-z]{4})?(?:-[A-Za-z]{2}|[0-9]{3})?(?:-(?:[A-Za-z0-9]{5,8}|[0-9][A-Za-z0-9]{3}))*(?:-[A-WY-Za-wy-z0-9](?:-[A-Za-z0-9]{2,8})+)*\\z",
                label="IETF BCP-47 language tag",
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_perspectives_mode_required_check() -> Check:
    return Check(
        field="names.rules[].perspectives.mode",
        name="required",
        expr=array_check(
            "names.rules",
            lambda el: F.when(
                el["perspectives"].isNotNull(),
                check_required(el["perspectives"]["mode"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_perspectives_mode_enum_check() -> Check:
    return Check(
        field="names.rules[].perspectives.mode",
        name="enum",
        expr=array_check(
            "names.rules",
            lambda el: check_enum(
                el["perspectives"]["mode"], ["accepted_by", "disputed_by"]
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_perspectives_countries_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries",
        name="required",
        expr=array_check(
            "names.rules",
            lambda el: F.when(
                el["perspectives"].isNotNull(),
                check_required(el["perspectives"]["countries"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_perspectives_countries_min_length_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries_min_length",
        name="array_min_length",
        expr=array_check(
            "names.rules",
            lambda el: check_array_min_length(el["perspectives"]["countries"], 1),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_perspectives_countries_unique_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries_unique",
        name="struct_unique",
        expr=array_check(
            "names.rules",
            lambda el: check_struct_unique(el["perspectives"]["countries"]),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_perspectives_countries_check_1() -> Check:
    return Check(
        field="names.rules[].perspectives.countries[]",
        name="country_code_alpha2",
        expr=nested_array_check(
            "names.rules",
            lambda el: array_check(
                el["perspectives"]["countries"],
                lambda inner: check_pattern(
                    inner, "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_between_linear_range_length_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_length",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_between_linear_range_bounds_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_bounds",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_between_linear_range_order_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_order",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_order(el["between"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def _names_rules_side_check() -> Check:
    return Check(
        field="names.rules[].side",
        name="enum",
        expr=array_check(
            "names.rules", lambda el: check_enum(el["side"], ["left", "right"])
        ),
        shape=CheckShape.ARRAY,
        read_columns=frozenset({"names"}),
    )


def place_checks() -> list[Check]:
    """All validation checks for place."""
    return [
        _id_required_check(),
        _id_string_min_length_check(),
        _id_no_whitespace_check(),
        _bbox_bbox_completeness_check(),
        _bbox_bbox_lat_ordering_check(),
        _bbox_bbox_lat_range_check(),
        _geometry_required_check(),
        _geometry_geometry_type_check(),
        _theme_required_check(),
        _theme_enum_check(),
        _type_required_check(),
        _type_enum_check(),
        _version_required_check(),
        _version_bounds_check(),
        _sources_min_length_check(),
        _sources_unique_check(),
        _sources_property_required_check(),
        _sources_property_json_pointer_check(),
        _sources_dataset_check(),
        _sources_license_check(),
        _sources_confidence_bounds_check(),
        _sources_confidence_bounds_check_1(),
        _sources_between_linear_range_length_check(),
        _sources_between_linear_range_bounds_check(),
        _sources_between_linear_range_order_check(),
        _operating_status_check(),
        _categories_primary_required_check(),
        _categories_primary_snake_case_check(),
        _categories_alternate_unique_check(),
        _categories_alternate_check(),
        _basic_category_check(),
        _taxonomy_primary_required_check(),
        _taxonomy_primary_snake_case_check(),
        _taxonomy_hierarchy_check(),
        _taxonomy_hierarchy_min_length_check(),
        _taxonomy_hierarchy_unique_check(),
        _taxonomy_hierarchy_check_1(),
        _taxonomy_alternates_min_length_check(),
        _taxonomy_alternates_unique_check(),
        _taxonomy_alternates_check(),
        _confidence_bounds_check(),
        _confidence_bounds_check_1(),
        _websites_min_length_check(),
        _websites_unique_check(),
        _websites_url_format_check(),
        _websites_url_length_check(),
        _socials_min_length_check(),
        _socials_unique_check(),
        _socials_url_format_check(),
        _socials_url_length_check(),
        _emails_min_length_check(),
        _emails_unique_check(),
        _emails_check(),
        _phones_min_length_check(),
        _phones_unique_check(),
        _phones_check(),
        _brand_names_primary_required_check(),
        _brand_names_primary_string_min_length_check(),
        _brand_names_primary_stripped_check(),
        _brand_names_common_key_check(),
        _brand_names_common_value_check(),
        _brand_names_rules_value_required_check(),
        _brand_names_rules_value_string_min_length_check(),
        _brand_names_rules_value_stripped_check(),
        _brand_names_rules_variant_required_check(),
        _brand_names_rules_variant_enum_check(),
        _brand_names_rules_language_check(),
        _brand_names_rules_perspectives_mode_required_check(),
        _brand_names_rules_perspectives_mode_enum_check(),
        _brand_names_rules_perspectives_countries_check(),
        _brand_names_rules_perspectives_countries_min_length_check(),
        _brand_names_rules_perspectives_countries_unique_check(),
        _brand_names_rules_perspectives_countries_check_1(),
        _brand_names_rules_between_linear_range_length_check(),
        _brand_names_rules_between_linear_range_bounds_check(),
        _brand_names_rules_between_linear_range_order_check(),
        _brand_names_rules_side_check(),
        _brand_wikidata_check(),
        _addresses_min_length_check(),
        _addresses_region_check(),
        _addresses_country_check(),
        _names_primary_required_check(),
        _names_primary_string_min_length_check(),
        _names_primary_stripped_check(),
        _names_common_key_check(),
        _names_common_value_check(),
        _names_rules_value_required_check(),
        _names_rules_value_string_min_length_check(),
        _names_rules_value_stripped_check(),
        _names_rules_variant_required_check(),
        _names_rules_variant_enum_check(),
        _names_rules_language_check(),
        _names_rules_perspectives_mode_required_check(),
        _names_rules_perspectives_mode_enum_check(),
        _names_rules_perspectives_countries_check(),
        _names_rules_perspectives_countries_min_length_check(),
        _names_rules_perspectives_countries_unique_check(),
        _names_rules_perspectives_countries_check_1(),
        _names_rules_between_linear_range_length_check(),
        _names_rules_between_linear_range_bounds_check(),
        _names_rules_between_linear_range_order_check(),
        _names_rules_side_check(),
    ]


PLACE_SCHEMA = StructType(
    [
        StructField("id", StringType(), True),
        StructField("bbox", BBOX_STRUCT, True),
        StructField("geometry", BinaryType(), True),
        StructField("theme", StringType(), True),
        StructField("type", StringType(), True),
        StructField("version", IntegerType(), True),
        StructField(
            "sources",
            ArrayType(
                StructType(
                    [
                        StructField("property", StringType(), True),
                        StructField("dataset", StringType(), True),
                        StructField("license", StringType(), True),
                        StructField("record_id", StringType(), True),
                        StructField("update_time", StringType(), True),
                        StructField("confidence", DoubleType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField("operating_status", StringType(), True),
        StructField(
            "categories",
            StructType(
                [
                    StructField("primary", StringType(), True),
                    StructField("alternate", ArrayType(StringType(), True), True),
                ]
            ),
            True,
        ),
        StructField("basic_category", StringType(), True),
        StructField(
            "taxonomy",
            StructType(
                [
                    StructField("primary", StringType(), True),
                    StructField("hierarchy", ArrayType(StringType(), True), True),
                    StructField("alternates", ArrayType(StringType(), True), True),
                ]
            ),
            True,
        ),
        StructField("confidence", DoubleType(), True),
        StructField("websites", ArrayType(StringType(), True), True),
        StructField("socials", ArrayType(StringType(), True), True),
        StructField("emails", ArrayType(StringType(), True), True),
        StructField("phones", ArrayType(StringType(), True), True),
        StructField(
            "brand",
            StructType(
                [
                    StructField(
                        "names",
                        StructType(
                            [
                                StructField("primary", StringType(), True),
                                StructField(
                                    "common",
                                    MapType(StringType(), StringType(), True),
                                    True,
                                ),
                                StructField(
                                    "rules",
                                    ArrayType(
                                        StructType(
                                            [
                                                StructField(
                                                    "value", StringType(), True
                                                ),
                                                StructField(
                                                    "variant", StringType(), True
                                                ),
                                                StructField(
                                                    "language", StringType(), True
                                                ),
                                                StructField(
                                                    "perspectives",
                                                    StructType(
                                                        [
                                                            StructField(
                                                                "mode",
                                                                StringType(),
                                                                True,
                                                            ),
                                                            StructField(
                                                                "countries",
                                                                ArrayType(
                                                                    StringType(), True
                                                                ),
                                                                True,
                                                            ),
                                                        ]
                                                    ),
                                                    True,
                                                ),
                                                StructField(
                                                    "between",
                                                    ArrayType(DoubleType(), True),
                                                    True,
                                                ),
                                                StructField("side", StringType(), True),
                                            ]
                                        ),
                                        True,
                                    ),
                                    True,
                                ),
                            ]
                        ),
                        True,
                    ),
                    StructField("wikidata", StringType(), True),
                ]
            ),
            True,
        ),
        StructField(
            "addresses",
            ArrayType(
                StructType(
                    [
                        StructField("freeform", StringType(), True),
                        StructField("locality", StringType(), True),
                        StructField("postcode", StringType(), True),
                        StructField("region", StringType(), True),
                        StructField("country", StringType(), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "names",
            StructType(
                [
                    StructField("primary", StringType(), True),
                    StructField(
                        "common", MapType(StringType(), StringType(), True), True
                    ),
                    StructField(
                        "rules",
                        ArrayType(
                            StructType(
                                [
                                    StructField("value", StringType(), True),
                                    StructField("variant", StringType(), True),
                                    StructField("language", StringType(), True),
                                    StructField(
                                        "perspectives",
                                        StructType(
                                            [
                                                StructField("mode", StringType(), True),
                                                StructField(
                                                    "countries",
                                                    ArrayType(StringType(), True),
                                                    True,
                                                ),
                                            ]
                                        ),
                                        True,
                                    ),
                                    StructField(
                                        "between", ArrayType(DoubleType(), True), True
                                    ),
                                    StructField("side", StringType(), True),
                                ]
                            ),
                            True,
                        ),
                        True,
                    ),
                ]
            ),
            True,
        ),
    ]
)

GEOMETRY_TYPES: tuple[GeometryType, ...] = (GeometryType.POINT,)

ENTRY_POINT = "overture.schema.places:Place"

PARTITIONS: dict[str, str] = {"theme": "places"}

MODEL_VALIDATION = ModelValidation(
    schema=PLACE_SCHEMA,
    checks=place_checks,
    geometry_types=GEOMETRY_TYPES,
)
