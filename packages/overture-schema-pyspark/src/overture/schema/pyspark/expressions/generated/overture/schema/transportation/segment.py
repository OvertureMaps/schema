# This file is auto-generated by overture-schema-codegen. Do not edit.

"""Segment validation expression builders."""

from __future__ import annotations

from pyspark.sql import functions as F
from pyspark.sql.types import (
    ArrayType,
    BinaryType,
    BooleanType,
    DoubleType,
    IntegerType,
    MapType,
    StringType,
    StructField,
    StructType,
)

from overture.schema.pyspark.check import Check, CheckShape, FeatureValidation
from overture.schema.pyspark.expressions._schema_structs import (
    BBOX_STRUCT,
)
from overture.schema.pyspark.expressions.column_patterns import (
    array_check,
    check_struct_unique,
    nested_array_check,
)
from overture.schema.pyspark.expressions.constraint_expressions import (
    check_array_min_length,
    check_bbox_completeness,
    check_bbox_lat_ordering,
    check_bbox_lat_range,
    check_bounds,
    check_enum,
    check_forbid_if,
    check_geometry_type,
    check_json_pointer,
    check_linear_range_bounds,
    check_linear_range_length,
    check_linear_range_order,
    check_pattern,
    check_require_any_of,
    check_require_if,
    check_required,
    check_string_min_length,
    check_stripped,
)
from overture.schema.system.primitive import GeometryType


def _id_required_check() -> Check:
    return Check(
        field="id",
        name="required",
        expr=check_required(F.col("id")),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _id_string_min_length_check() -> Check:
    return Check(
        field="id",
        name="string_min_length",
        expr=check_string_min_length(F.col("id"), 1),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _id_no_whitespace_check() -> Check:
    return Check(
        field="id",
        name="no_whitespace",
        expr=check_pattern(
            F.col("id"), "^\\S+\\z", label="String without whitespace characters"
        ),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _bbox_bbox_completeness_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_completeness",
        expr=check_bbox_completeness(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _bbox_bbox_lat_ordering_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_lat_ordering",
        expr=check_bbox_lat_ordering(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _bbox_bbox_lat_range_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_lat_range",
        expr=check_bbox_lat_range(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _geometry_required_check() -> Check:
    return Check(
        field="geometry",
        name="required",
        expr=check_required(F.col("geometry")),
        shape=CheckShape.SCALAR,
        root_field="geometry",
    )


def _geometry_geometry_type_check() -> Check:
    return Check(
        field="geometry",
        name="geometry_type",
        expr=check_geometry_type(F.col("geometry"), GeometryType.LINE_STRING),
        shape=CheckShape.SCALAR,
        root_field="geometry",
    )


def _theme_required_check() -> Check:
    return Check(
        field="theme",
        name="required",
        expr=check_required(F.col("theme")),
        shape=CheckShape.SCALAR,
        root_field="theme",
    )


def _theme_enum_check() -> Check:
    return Check(
        field="theme",
        name="enum",
        expr=check_enum(F.col("theme"), ["transportation"]),
        shape=CheckShape.SCALAR,
        root_field="theme",
    )


def _type_required_check() -> Check:
    return Check(
        field="type",
        name="required",
        expr=check_required(F.col("type")),
        shape=CheckShape.SCALAR,
        root_field="type",
    )


def _type_enum_check() -> Check:
    return Check(
        field="type",
        name="enum",
        expr=check_enum(F.col("type"), ["segment"]),
        shape=CheckShape.SCALAR,
        root_field="type",
    )


def _version_required_check() -> Check:
    return Check(
        field="version",
        name="required",
        expr=check_required(F.col("version")),
        shape=CheckShape.SCALAR,
        root_field="version",
    )


def _version_bounds_check() -> Check:
    return Check(
        field="version",
        name="bounds",
        expr=check_bounds(F.col("version"), ge=0),
        shape=CheckShape.SCALAR,
        root_field="version",
    )


def _sources_min_length_check() -> Check:
    return Check(
        field="sources_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("sources"), 1),
        shape=CheckShape.SCALAR,
        root_field="sources",
    )


def _sources_unique_check() -> Check:
    return Check(
        field="sources_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("sources")),
        shape=CheckShape.SCALAR,
        root_field="sources",
    )


def _sources_property_required_check() -> Check:
    return Check(
        field="sources[].property",
        name="required",
        expr=array_check("sources", lambda el: check_required(el["property"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_property_json_pointer_check() -> Check:
    return Check(
        field="sources[].property",
        name="json_pointer",
        expr=array_check("sources", lambda el: check_json_pointer(el["property"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_dataset_check() -> Check:
    return Check(
        field="sources[].dataset",
        name="required",
        expr=array_check("sources", lambda el: check_required(el["dataset"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_license_check() -> Check:
    return Check(
        field="sources[].license",
        name="stripped",
        expr=array_check("sources", lambda el: check_stripped(el["license"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_confidence_bounds_check() -> Check:
    return Check(
        field="sources[].confidence",
        name="bounds",
        expr=array_check("sources", lambda el: check_bounds(el["confidence"], ge=0.0)),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_confidence_bounds_check_1() -> Check:
    return Check(
        field="sources[].confidence",
        name="bounds",
        expr=array_check("sources", lambda el: check_bounds(el["confidence"], le=1.0)),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_length_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_length",
        expr=array_check(
            "sources", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_bounds_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_bounds",
        expr=array_check(
            "sources", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_order_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_order",
        expr=array_check("sources", lambda el: check_linear_range_order(el["between"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _subtype_required_check() -> Check:
    return Check(
        field="subtype",
        name="required",
        expr=check_required(F.col("subtype")),
        shape=CheckShape.SCALAR,
        root_field="subtype",
    )


def _subtype_enum_check() -> Check:
    return Check(
        field="subtype",
        name="enum",
        expr=check_enum(F.col("subtype"), ["road", "rail", "water"]),
        shape=CheckShape.SCALAR,
        root_field="subtype",
    )


def _access_restrictions_min_length_check() -> Check:
    return Check(
        field="access_restrictions_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("access_restrictions"), 1),
        shape=CheckShape.SCALAR,
        root_field="access_restrictions",
    )


def _access_restrictions_unique_check() -> Check:
    return Check(
        field="access_restrictions_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("access_restrictions")),
        shape=CheckShape.SCALAR,
        root_field="access_restrictions",
    )


def _access_restrictions_access_type_required_check() -> Check:
    return Check(
        field="access_restrictions[].access_type",
        name="required",
        expr=array_check(
            "access_restrictions", lambda el: check_required(el["access_type"])
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_access_type_enum_check() -> Check:
    return Check(
        field="access_restrictions[].access_type",
        name="enum",
        expr=array_check(
            "access_restrictions",
            lambda el: check_enum(
                el["access_type"], ["allowed", "denied", "designated"]
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_between_linear_range_length_check() -> Check:
    return Check(
        field="access_restrictions[].between",
        name="linear_range_length",
        expr=array_check(
            "access_restrictions", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_between_linear_range_bounds_check() -> Check:
    return Check(
        field="access_restrictions[].between",
        name="linear_range_bounds",
        expr=array_check(
            "access_restrictions", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_between_linear_range_order_check() -> Check:
    return Check(
        field="access_restrictions[].between",
        name="linear_range_order",
        expr=array_check(
            "access_restrictions", lambda el: check_linear_range_order(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_heading_check() -> Check:
    return Check(
        field="access_restrictions[].when.heading",
        name="enum",
        expr=array_check(
            "access_restrictions",
            lambda el: check_enum(el["when"]["heading"], ["forward", "backward"]),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_mode_min_length_check() -> Check:
    return Check(
        field="access_restrictions[].when.mode_min_length",
        name="array_min_length",
        expr=array_check(
            "access_restrictions",
            lambda el: check_array_min_length(el["when"]["mode"], 1),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_mode_unique_check() -> Check:
    return Check(
        field="access_restrictions[].when.mode_unique",
        name="struct_unique",
        expr=array_check(
            "access_restrictions", lambda el: check_struct_unique(el["when"]["mode"])
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_mode_check() -> Check:
    return Check(
        field="access_restrictions[].when.mode[]",
        name="enum",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["mode"],
                lambda inner: check_enum(
                    inner,
                    [
                        "vehicle",
                        "motor_vehicle",
                        "car",
                        "truck",
                        "motorcycle",
                        "foot",
                        "bicycle",
                        "bus",
                        "hgv",
                        "hov",
                        "emergency",
                    ],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_using_min_length_check() -> Check:
    return Check(
        field="access_restrictions[].when.using_min_length",
        name="array_min_length",
        expr=array_check(
            "access_restrictions",
            lambda el: check_array_min_length(el["when"]["using"], 1),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_using_unique_check() -> Check:
    return Check(
        field="access_restrictions[].when.using_unique",
        name="struct_unique",
        expr=array_check(
            "access_restrictions", lambda el: check_struct_unique(el["when"]["using"])
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_using_check() -> Check:
    return Check(
        field="access_restrictions[].when.using[]",
        name="enum",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["using"],
                lambda inner: check_enum(
                    inner,
                    [
                        "as_customer",
                        "at_destination",
                        "to_deliver",
                        "to_farm",
                        "for_forestry",
                    ],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_recognized_min_length_check() -> Check:
    return Check(
        field="access_restrictions[].when.recognized_min_length",
        name="array_min_length",
        expr=array_check(
            "access_restrictions",
            lambda el: check_array_min_length(el["when"]["recognized"], 1),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_recognized_unique_check() -> Check:
    return Check(
        field="access_restrictions[].when.recognized_unique",
        name="struct_unique",
        expr=array_check(
            "access_restrictions",
            lambda el: check_struct_unique(el["when"]["recognized"]),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_recognized_check() -> Check:
    return Check(
        field="access_restrictions[].when.recognized[]",
        name="enum",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["recognized"],
                lambda inner: check_enum(
                    inner,
                    [
                        "as_permitted",
                        "as_private",
                        "as_disabled",
                        "as_employee",
                        "as_student",
                    ],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_min_length_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle_min_length",
        name="array_min_length",
        expr=array_check(
            "access_restrictions",
            lambda el: check_array_min_length(el["when"]["vehicle"], 1),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_unique_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle_unique",
        name="struct_unique",
        expr=array_check(
            "access_restrictions", lambda el: check_struct_unique(el["when"]["vehicle"])
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_dimension_required_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].dimension",
        name="required",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"], lambda inner: check_required(inner["dimension"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_dimension_enum_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].dimension",
        name="enum",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_enum(
                    inner["dimension"],
                    ["axle_count", "height", "length", "weight", "width"],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_comparison_required_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].comparison",
        name="required",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"], lambda inner: check_required(inner["comparison"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_comparison_enum_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].comparison",
        name="enum",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_enum(
                    inner["comparison"],
                    [
                        "greater_than",
                        "greater_than_equal",
                        "equal",
                        "less_than",
                        "less_than_equal",
                    ],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_value_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].value",
        name="required",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: F.when(
                    inner["dimension"].isin(["axle_count"]),
                    check_required(inner["value"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_value_required_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].value",
        name="required",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: F.when(
                    inner["dimension"].isin(["height", "length", "weight", "width"]),
                    check_required(inner["value"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_value_bounds_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].value",
        name="bounds",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: F.when(
                    inner["dimension"].isin(["height", "length", "weight", "width"]),
                    check_bounds(inner["value"], ge=0.0),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_unit_required_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit",
        name="required",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: F.when(
                    inner["dimension"].isin(["height", "length", "width"]),
                    check_required(inner["unit"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_unit_enum_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit",
        name="enum",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: F.when(
                    inner["dimension"].isin(["height", "length", "width"]),
                    check_enum(
                        inner["unit"], ["in", "ft", "yd", "mi", "cm", "m", "km"]
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_unit_required_check_1() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit",
        name="required",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: F.when(
                    inner["dimension"].isin(["weight"]), check_required(inner["unit"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_unit_enum_check_1() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit",
        name="enum",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: F.when(
                    inner["dimension"].isin(["weight"]),
                    check_enum(inner["unit"], ["oz", "lb", "st", "lt", "g", "kg", "t"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _connectors_min_length_check() -> Check:
    return Check(
        field="connectors_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("connectors"), 2),
        shape=CheckShape.SCALAR,
        root_field="connectors",
    )


def _connectors_unique_check() -> Check:
    return Check(
        field="connectors_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("connectors")),
        shape=CheckShape.SCALAR,
        root_field="connectors",
    )


def _connectors_connector_id_required_check() -> Check:
    return Check(
        field="connectors[].connector_id",
        name="required",
        expr=array_check("connectors", lambda el: check_required(el["connector_id"])),
        shape=CheckShape.ARRAY,
        root_field="connectors",
    )


def _connectors_connector_id_string_min_length_check() -> Check:
    return Check(
        field="connectors[].connector_id",
        name="string_min_length",
        expr=array_check(
            "connectors", lambda el: check_string_min_length(el["connector_id"], 1)
        ),
        shape=CheckShape.ARRAY,
        root_field="connectors",
    )


def _connectors_connector_id_no_whitespace_check() -> Check:
    return Check(
        field="connectors[].connector_id",
        name="no_whitespace",
        expr=array_check(
            "connectors",
            lambda el: check_pattern(
                el["connector_id"],
                "^\\S+\\z",
                label="String without whitespace characters",
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="connectors",
    )


def _connectors_at_bounds_check() -> Check:
    return Check(
        field="connectors[].at",
        name="bounds",
        expr=array_check("connectors", lambda el: check_bounds(el["at"], ge=0.0)),
        shape=CheckShape.ARRAY,
        root_field="connectors",
    )


def _connectors_at_bounds_check_1() -> Check:
    return Check(
        field="connectors[].at",
        name="bounds",
        expr=array_check("connectors", lambda el: check_bounds(el["at"], le=1.0)),
        shape=CheckShape.ARRAY,
        root_field="connectors",
    )


def _level_rules_value_check() -> Check:
    return Check(
        field="level_rules[].value",
        name="required",
        expr=array_check("level_rules", lambda el: check_required(el["value"])),
        shape=CheckShape.ARRAY,
        root_field="level_rules",
    )


def _level_rules_between_linear_range_length_check() -> Check:
    return Check(
        field="level_rules[].between",
        name="linear_range_length",
        expr=array_check(
            "level_rules", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="level_rules",
    )


def _level_rules_between_linear_range_bounds_check() -> Check:
    return Check(
        field="level_rules[].between",
        name="linear_range_bounds",
        expr=array_check(
            "level_rules", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="level_rules",
    )


def _level_rules_between_linear_range_order_check() -> Check:
    return Check(
        field="level_rules[].between",
        name="linear_range_order",
        expr=array_check(
            "level_rules", lambda el: check_linear_range_order(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="level_rules",
    )


def _routes_name_string_min_length_check() -> Check:
    return Check(
        field="routes[].name",
        name="string_min_length",
        expr=array_check("routes", lambda el: check_string_min_length(el["name"], 1)),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_name_stripped_check() -> Check:
    return Check(
        field="routes[].name",
        name="stripped",
        expr=array_check("routes", lambda el: check_stripped(el["name"])),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_network_string_min_length_check() -> Check:
    return Check(
        field="routes[].network",
        name="string_min_length",
        expr=array_check(
            "routes", lambda el: check_string_min_length(el["network"], 1)
        ),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_network_stripped_check() -> Check:
    return Check(
        field="routes[].network",
        name="stripped",
        expr=array_check("routes", lambda el: check_stripped(el["network"])),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_ref_string_min_length_check() -> Check:
    return Check(
        field="routes[].ref",
        name="string_min_length",
        expr=array_check("routes", lambda el: check_string_min_length(el["ref"], 1)),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_ref_stripped_check() -> Check:
    return Check(
        field="routes[].ref",
        name="stripped",
        expr=array_check("routes", lambda el: check_stripped(el["ref"])),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_symbol_string_min_length_check() -> Check:
    return Check(
        field="routes[].symbol",
        name="string_min_length",
        expr=array_check("routes", lambda el: check_string_min_length(el["symbol"], 1)),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_symbol_stripped_check() -> Check:
    return Check(
        field="routes[].symbol",
        name="stripped",
        expr=array_check("routes", lambda el: check_stripped(el["symbol"])),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_wikidata_check() -> Check:
    return Check(
        field="routes[].wikidata",
        name="wikidata_id",
        expr=array_check(
            "routes",
            lambda el: check_pattern(
                el["wikidata"],
                "^Q\\d+\\z",
                label="Wikidata identifier (Q followed by digits)",
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_between_linear_range_length_check() -> Check:
    return Check(
        field="routes[].between",
        name="linear_range_length",
        expr=array_check("routes", lambda el: check_linear_range_length(el["between"])),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_between_linear_range_bounds_check() -> Check:
    return Check(
        field="routes[].between",
        name="linear_range_bounds",
        expr=array_check("routes", lambda el: check_linear_range_bounds(el["between"])),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _routes_between_linear_range_order_check() -> Check:
    return Check(
        field="routes[].between",
        name="linear_range_order",
        expr=array_check("routes", lambda el: check_linear_range_order(el["between"])),
        shape=CheckShape.ARRAY,
        root_field="routes",
    )


def _subclass_rules_value_required_check() -> Check:
    return Check(
        field="subclass_rules[].value",
        name="required",
        expr=array_check("subclass_rules", lambda el: check_required(el["value"])),
        shape=CheckShape.ARRAY,
        root_field="subclass_rules",
    )


def _subclass_rules_value_enum_check() -> Check:
    return Check(
        field="subclass_rules[].value",
        name="enum",
        expr=array_check(
            "subclass_rules",
            lambda el: check_enum(
                el["value"],
                [
                    "link",
                    "sidewalk",
                    "crosswalk",
                    "parking_aisle",
                    "driveway",
                    "alley",
                    "cycle_crossing",
                ],
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="subclass_rules",
    )


def _subclass_rules_between_linear_range_length_check() -> Check:
    return Check(
        field="subclass_rules[].between",
        name="linear_range_length",
        expr=array_check(
            "subclass_rules", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="subclass_rules",
    )


def _subclass_rules_between_linear_range_bounds_check() -> Check:
    return Check(
        field="subclass_rules[].between",
        name="linear_range_bounds",
        expr=array_check(
            "subclass_rules", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="subclass_rules",
    )


def _subclass_rules_between_linear_range_order_check() -> Check:
    return Check(
        field="subclass_rules[].between",
        name="linear_range_order",
        expr=array_check(
            "subclass_rules", lambda el: check_linear_range_order(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="subclass_rules",
    )


def _names_primary_required_check() -> Check:
    return Check(
        field="names.primary",
        name="required",
        expr=F.when(F.col("names").isNotNull(), check_required(F.col("names.primary"))),
        shape=CheckShape.SCALAR,
        root_field="names",
    )


def _names_primary_string_min_length_check() -> Check:
    return Check(
        field="names.primary",
        name="string_min_length",
        expr=check_string_min_length(F.col("names.primary"), 1),
        shape=CheckShape.SCALAR,
        root_field="names",
    )


def _names_primary_stripped_check() -> Check:
    return Check(
        field="names.primary",
        name="stripped",
        expr=check_stripped(F.col("names.primary")),
        shape=CheckShape.SCALAR,
        root_field="names",
    )


def _names_rules_value_required_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="required",
        expr=array_check("names.rules", lambda el: check_required(el["value"])),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_value_string_min_length_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="string_min_length",
        expr=array_check(
            "names.rules", lambda el: check_string_min_length(el["value"], 1)
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_value_stripped_check() -> Check:
    return Check(
        field="names.rules[].value",
        name="stripped",
        expr=array_check("names.rules", lambda el: check_stripped(el["value"])),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_variant_required_check() -> Check:
    return Check(
        field="names.rules[].variant",
        name="required",
        expr=array_check("names.rules", lambda el: check_required(el["variant"])),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_variant_enum_check() -> Check:
    return Check(
        field="names.rules[].variant",
        name="enum",
        expr=array_check(
            "names.rules",
            lambda el: check_enum(
                el["variant"], ["common", "official", "alternate", "short"]
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_language_check() -> Check:
    return Check(
        field="names.rules[].language",
        name="language_tag",
        expr=array_check(
            "names.rules",
            lambda el: check_pattern(
                el["language"],
                "^(?:(?:[A-Za-z]{2,3}(?:-[A-Za-z]{3}){0,3}?)|(?:[A-Za-z]{4,8}))(?:-[A-Za-z]{4})?(?:-[A-Za-z]{2}|[0-9]{3})?(?:-(?:[A-Za-z0-9]{5,8}|[0-9][A-Za-z0-9]{3}))*(?:-[A-WY-Za-wy-z0-9](?:-[A-Za-z0-9]{2,8})+)*\\z",
                label="IETF BCP-47 language tag",
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_mode_required_check() -> Check:
    return Check(
        field="names.rules[].perspectives.mode",
        name="required",
        expr=array_check(
            "names.rules",
            lambda el: F.when(
                el["perspectives"].isNotNull(),
                check_required(el["perspectives"]["mode"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_mode_enum_check() -> Check:
    return Check(
        field="names.rules[].perspectives.mode",
        name="enum",
        expr=array_check(
            "names.rules",
            lambda el: check_enum(
                el["perspectives"]["mode"], ["accepted_by", "disputed_by"]
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_countries_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries",
        name="required",
        expr=array_check(
            "names.rules",
            lambda el: F.when(
                el["perspectives"].isNotNull(),
                check_required(el["perspectives"]["countries"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_countries_min_length_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries_min_length",
        name="array_min_length",
        expr=array_check(
            "names.rules",
            lambda el: check_array_min_length(el["perspectives"]["countries"], 1),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_countries_unique_check() -> Check:
    return Check(
        field="names.rules[].perspectives.countries_unique",
        name="struct_unique",
        expr=array_check(
            "names.rules",
            lambda el: check_struct_unique(el["perspectives"]["countries"]),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_perspectives_countries_check_1() -> Check:
    return Check(
        field="names.rules[].perspectives.countries[]",
        name="country_code_alpha2",
        expr=nested_array_check(
            "names.rules",
            lambda el: array_check(
                el["perspectives"]["countries"],
                lambda inner: check_pattern(
                    inner, "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_between_linear_range_length_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_length",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_between_linear_range_bounds_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_bounds",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_between_linear_range_order_check() -> Check:
    return Check(
        field="names.rules[].between",
        name="linear_range_order",
        expr=array_check(
            "names.rules", lambda el: check_linear_range_order(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _names_rules_side_check() -> Check:
    return Check(
        field="names.rules[].side",
        name="enum",
        expr=array_check(
            "names.rules", lambda el: check_enum(el["side"], ["left", "right"])
        ),
        shape=CheckShape.ARRAY,
        root_field="names",
    )


def _class_required_check() -> Check:
    return Check(
        field="class",
        name="required",
        expr=F.when(F.col("subtype").isin(["road"]), check_required(F.col("class"))),
        shape=CheckShape.SCALAR,
        root_field="class",
    )


def _class_enum_check() -> Check:
    return Check(
        field="class",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            check_enum(
                F.col("class"),
                [
                    "motorway",
                    "primary",
                    "secondary",
                    "tertiary",
                    "residential",
                    "living_street",
                    "trunk",
                    "unclassified",
                    "service",
                    "pedestrian",
                    "footway",
                    "steps",
                    "path",
                    "track",
                    "cycleway",
                    "bridleway",
                    "unknown",
                ],
            ),
        ),
        shape=CheckShape.SCALAR,
        root_field="class",
    )


def _destinations_from_connector_id_required_check() -> Check:
    return Check(
        field="destinations[].from_connector_id",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations", lambda el: check_required(el["from_connector_id"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_from_connector_id_string_min_length_check() -> Check:
    return Check(
        field="destinations[].from_connector_id",
        name="string_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: check_string_min_length(el["from_connector_id"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_from_connector_id_no_whitespace_check() -> Check:
    return Check(
        field="destinations[].from_connector_id",
        name="no_whitespace",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: check_pattern(
                    el["from_connector_id"],
                    "^\\S+\\z",
                    label="String without whitespace characters",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_to_connector_id_required_check() -> Check:
    return Check(
        field="destinations[].to_connector_id",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations", lambda el: check_required(el["to_connector_id"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_to_connector_id_string_min_length_check() -> Check:
    return Check(
        field="destinations[].to_connector_id",
        name="string_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: check_string_min_length(el["to_connector_id"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_to_connector_id_no_whitespace_check() -> Check:
    return Check(
        field="destinations[].to_connector_id",
        name="no_whitespace",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: check_pattern(
                    el["to_connector_id"],
                    "^\\S+\\z",
                    label="String without whitespace characters",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_to_segment_id_required_check() -> Check:
    return Check(
        field="destinations[].to_segment_id",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("destinations", lambda el: check_required(el["to_segment_id"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_to_segment_id_string_min_length_check() -> Check:
    return Check(
        field="destinations[].to_segment_id",
        name="string_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: check_string_min_length(el["to_segment_id"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_to_segment_id_no_whitespace_check() -> Check:
    return Check(
        field="destinations[].to_segment_id",
        name="no_whitespace",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: check_pattern(
                    el["to_segment_id"],
                    "^\\S+\\z",
                    label="String without whitespace characters",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_final_heading_required_check() -> Check:
    return Check(
        field="destinations[].final_heading",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("destinations", lambda el: check_required(el["final_heading"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_final_heading_enum_check() -> Check:
    return Check(
        field="destinations[].final_heading",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: check_enum(el["final_heading"], ["forward", "backward"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_labels_min_length_check() -> Check:
    return Check(
        field="destinations[].labels_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations", lambda el: check_array_min_length(el["labels"], 1)
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_labels_unique_check() -> Check:
    return Check(
        field="destinations[].labels_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("destinations", lambda el: check_struct_unique(el["labels"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_labels_value_required_check() -> Check:
    return Check(
        field="destinations[].labels[].value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "destinations",
                lambda el: array_check(
                    el["labels"], lambda inner: check_required(inner["value"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_labels_value_string_min_length_check() -> Check:
    return Check(
        field="destinations[].labels[].value",
        name="string_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "destinations",
                lambda el: array_check(
                    el["labels"],
                    lambda inner: check_string_min_length(inner["value"], 1),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_labels_value_stripped_check() -> Check:
    return Check(
        field="destinations[].labels[].value",
        name="stripped",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "destinations",
                lambda el: array_check(
                    el["labels"], lambda inner: check_stripped(inner["value"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_labels_type_required_check() -> Check:
    return Check(
        field="destinations[].labels[].type",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "destinations",
                lambda el: array_check(
                    el["labels"], lambda inner: check_required(inner["type"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_labels_type_enum_check() -> Check:
    return Check(
        field="destinations[].labels[].type",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "destinations",
                lambda el: array_check(
                    el["labels"],
                    lambda inner: check_enum(
                        inner["type"],
                        [
                            "street",
                            "country",
                            "route_ref",
                            "toward_route_ref",
                            "unknown",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_symbols_unique_check() -> Check:
    return Check(
        field="destinations[].symbols_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("destinations", lambda el: check_struct_unique(el["symbols"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_symbols_check() -> Check:
    return Check(
        field="destinations[].symbols[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "destinations",
                lambda el: array_check(
                    el["symbols"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "motorway",
                            "airport",
                            "hospital",
                            "center",
                            "industrial",
                            "parking",
                            "bus",
                            "train_station",
                            "rest_area",
                            "ferry",
                            "motorroad",
                            "fuel",
                            "viewpoint",
                            "fuel_diesel",
                            "food",
                            "lodging",
                            "info",
                            "camp_site",
                            "interchange",
                            "restrooms",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_when_heading_required_check() -> Check:
    return Check(
        field="destinations[].when.heading",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: F.when(
                    el["when"].isNotNull(), check_required(el["when"]["heading"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _destinations_when_heading_enum_check() -> Check:
    return Check(
        field="destinations[].when.heading",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "destinations",
                lambda el: check_enum(el["when"]["heading"], ["forward", "backward"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _prohibited_transitions_sequence_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions", lambda el: check_required(el["sequence"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_sequence_min_length_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_array_min_length(el["sequence"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_sequence_unique_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions", lambda el: check_struct_unique(el["sequence"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_sequence_connector_id_required_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence[].connector_id",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["sequence"], lambda inner: check_required(inner["connector_id"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_sequence_connector_id_string_min_length_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence[].connector_id",
        name="string_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["sequence"],
                    lambda inner: check_string_min_length(inner["connector_id"], 1),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_sequence_connector_id_no_whitespace_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence[].connector_id",
        name="no_whitespace",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["sequence"],
                    lambda inner: check_pattern(
                        inner["connector_id"],
                        "^\\S+\\z",
                        label="String without whitespace characters",
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_sequence_segment_id_required_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence[].segment_id",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["sequence"], lambda inner: check_required(inner["segment_id"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_sequence_segment_id_string_min_length_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence[].segment_id",
        name="string_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["sequence"],
                    lambda inner: check_string_min_length(inner["segment_id"], 1),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_sequence_segment_id_no_whitespace_check() -> Check:
    return Check(
        field="prohibited_transitions[].sequence[].segment_id",
        name="no_whitespace",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["sequence"],
                    lambda inner: check_pattern(
                        inner["segment_id"],
                        "^\\S+\\z",
                        label="String without whitespace characters",
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_final_heading_required_check() -> Check:
    return Check(
        field="prohibited_transitions[].final_heading",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions", lambda el: check_required(el["final_heading"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_final_heading_enum_check() -> Check:
    return Check(
        field="prohibited_transitions[].final_heading",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_enum(el["final_heading"], ["forward", "backward"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_between_linear_range_length_check() -> Check:
    return Check(
        field="prohibited_transitions[].between",
        name="linear_range_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_linear_range_length(el["between"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_between_linear_range_bounds_check() -> Check:
    return Check(
        field="prohibited_transitions[].between",
        name="linear_range_bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_linear_range_bounds(el["between"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_between_linear_range_order_check() -> Check:
    return Check(
        field="prohibited_transitions[].between",
        name="linear_range_order",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_linear_range_order(el["between"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_heading_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.heading",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_enum(el["when"]["heading"], ["forward", "backward"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_mode_min_length_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.mode_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_array_min_length(el["when"]["mode"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_mode_unique_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.mode_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_struct_unique(el["when"]["mode"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_mode_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.mode[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["mode"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "vehicle",
                            "motor_vehicle",
                            "car",
                            "truck",
                            "motorcycle",
                            "foot",
                            "bicycle",
                            "bus",
                            "hgv",
                            "hov",
                            "emergency",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_using_min_length_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.using_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_array_min_length(el["when"]["using"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_using_unique_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.using_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_struct_unique(el["when"]["using"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_using_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.using[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["using"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "as_customer",
                            "at_destination",
                            "to_deliver",
                            "to_farm",
                            "for_forestry",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_recognized_min_length_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.recognized_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_array_min_length(el["when"]["recognized"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_recognized_unique_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.recognized_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_struct_unique(el["when"]["recognized"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_recognized_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.recognized[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["recognized"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "as_permitted",
                            "as_private",
                            "as_disabled",
                            "as_employee",
                            "as_student",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_min_length_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_array_min_length(el["when"]["vehicle"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_unique_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "prohibited_transitions",
                lambda el: check_struct_unique(el["when"]["vehicle"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_dimension_required_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].dimension",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: check_required(inner["dimension"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_dimension_enum_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].dimension",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: check_enum(
                        inner["dimension"],
                        ["axle_count", "height", "length", "weight", "width"],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_comparison_required_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].comparison",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: check_required(inner["comparison"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_comparison_enum_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].comparison",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: check_enum(
                        inner["comparison"],
                        [
                            "greater_than",
                            "greater_than_equal",
                            "equal",
                            "less_than",
                            "less_than_equal",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_value_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["axle_count"]),
                        check_required(inner["value"]),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_value_required_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(
                            ["height", "length", "weight", "width"]
                        ),
                        check_required(inner["value"]),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_value_bounds_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].value",
        name="bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(
                            ["height", "length", "weight", "width"]
                        ),
                        check_bounds(inner["value"], ge=0.0),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_unit_required_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["height", "length", "width"]),
                        check_required(inner["unit"]),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_unit_enum_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["height", "length", "width"]),
                        check_enum(
                            inner["unit"], ["in", "ft", "yd", "mi", "cm", "m", "km"]
                        ),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_unit_required_check_1() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["weight"]),
                        check_required(inner["unit"]),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_unit_enum_check_1() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "prohibited_transitions",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["weight"]),
                        check_enum(
                            inner["unit"], ["oz", "lb", "st", "lt", "g", "kg", "t"]
                        ),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _road_flags_min_length_check() -> Check:
    return Check(
        field="road_flags_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            check_array_min_length(F.col("road_flags"), 1),
        ),
        shape=CheckShape.SCALAR,
        root_field="road_flags",
    )


def _road_flags_unique_check() -> Check:
    return Check(
        field="road_flags_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]), check_struct_unique(F.col("road_flags"))
        ),
        shape=CheckShape.SCALAR,
        root_field="road_flags",
    )


def _road_flags_values_check() -> Check:
    return Check(
        field="road_flags[].values",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("road_flags", lambda el: check_required(el["values"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_flags",
    )


def _road_flags_values_min_length_check() -> Check:
    return Check(
        field="road_flags[].values_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "road_flags", lambda el: check_array_min_length(el["values"], 1)
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_flags",
    )


def _road_flags_values_unique_check() -> Check:
    return Check(
        field="road_flags[].values_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("road_flags", lambda el: check_struct_unique(el["values"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_flags",
    )


def _road_flags_values_check_1() -> Check:
    return Check(
        field="road_flags[].values[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "road_flags",
                lambda el: array_check(
                    el["values"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "is_bridge",
                            "is_link",
                            "is_tunnel",
                            "is_under_construction",
                            "is_abandoned",
                            "is_covered",
                            "is_indoor",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_flags",
    )


def _road_flags_between_linear_range_length_check() -> Check:
    return Check(
        field="road_flags[].between",
        name="linear_range_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "road_flags", lambda el: check_linear_range_length(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_flags",
    )


def _road_flags_between_linear_range_bounds_check() -> Check:
    return Check(
        field="road_flags[].between",
        name="linear_range_bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "road_flags", lambda el: check_linear_range_bounds(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_flags",
    )


def _road_flags_between_linear_range_order_check() -> Check:
    return Check(
        field="road_flags[].between",
        name="linear_range_order",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "road_flags", lambda el: check_linear_range_order(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_flags",
    )


def _road_surface_min_length_check() -> Check:
    return Check(
        field="road_surface_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            check_array_min_length(F.col("road_surface"), 1),
        ),
        shape=CheckShape.SCALAR,
        root_field="road_surface",
    )


def _road_surface_unique_check() -> Check:
    return Check(
        field="road_surface_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]), check_struct_unique(F.col("road_surface"))
        ),
        shape=CheckShape.SCALAR,
        root_field="road_surface",
    )


def _road_surface_value_required_check() -> Check:
    return Check(
        field="road_surface[].value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("road_surface", lambda el: check_required(el["value"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_surface",
    )


def _road_surface_value_enum_check() -> Check:
    return Check(
        field="road_surface[].value",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "road_surface",
                lambda el: check_enum(
                    el["value"],
                    [
                        "unknown",
                        "paved",
                        "unpaved",
                        "gravel",
                        "dirt",
                        "paving_stones",
                        "metal",
                    ],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_surface",
    )


def _road_surface_between_linear_range_length_check() -> Check:
    return Check(
        field="road_surface[].between",
        name="linear_range_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "road_surface", lambda el: check_linear_range_length(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_surface",
    )


def _road_surface_between_linear_range_bounds_check() -> Check:
    return Check(
        field="road_surface[].between",
        name="linear_range_bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "road_surface", lambda el: check_linear_range_bounds(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_surface",
    )


def _road_surface_between_linear_range_order_check() -> Check:
    return Check(
        field="road_surface[].between",
        name="linear_range_order",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "road_surface", lambda el: check_linear_range_order(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="road_surface",
    )


def _speed_limits_min_length_check() -> Check:
    return Check(
        field="speed_limits_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            check_array_min_length(F.col("speed_limits"), 1),
        ),
        shape=CheckShape.SCALAR,
        root_field="speed_limits",
    )


def _speed_limits_unique_check() -> Check:
    return Check(
        field="speed_limits_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]), check_struct_unique(F.col("speed_limits"))
        ),
        shape=CheckShape.SCALAR,
        root_field="speed_limits",
    )


def _speed_limits_max_speed_value_required_check() -> Check:
    return Check(
        field="speed_limits[].max_speed.value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: F.when(
                    el["max_speed"].isNotNull(),
                    check_required(el["max_speed"]["value"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_max_speed_value_bounds_check() -> Check:
    return Check(
        field="speed_limits[].max_speed.value",
        name="bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_bounds(el["max_speed"]["value"], ge=1)
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_max_speed_value_bounds_check_1() -> Check:
    return Check(
        field="speed_limits[].max_speed.value",
        name="bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: check_bounds(el["max_speed"]["value"], le=350),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_max_speed_unit_required_check() -> Check:
    return Check(
        field="speed_limits[].max_speed.unit",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: F.when(
                    el["max_speed"].isNotNull(), check_required(el["max_speed"]["unit"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_max_speed_unit_enum_check() -> Check:
    return Check(
        field="speed_limits[].max_speed.unit",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: check_enum(el["max_speed"]["unit"], ["mph", "km/h"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_min_speed_value_required_check() -> Check:
    return Check(
        field="speed_limits[].min_speed.value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: F.when(
                    el["min_speed"].isNotNull(),
                    check_required(el["min_speed"]["value"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_min_speed_value_bounds_check() -> Check:
    return Check(
        field="speed_limits[].min_speed.value",
        name="bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_bounds(el["min_speed"]["value"], ge=1)
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_min_speed_value_bounds_check_1() -> Check:
    return Check(
        field="speed_limits[].min_speed.value",
        name="bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: check_bounds(el["min_speed"]["value"], le=350),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_min_speed_unit_required_check() -> Check:
    return Check(
        field="speed_limits[].min_speed.unit",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: F.when(
                    el["min_speed"].isNotNull(), check_required(el["min_speed"]["unit"])
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_min_speed_unit_enum_check() -> Check:
    return Check(
        field="speed_limits[].min_speed.unit",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: check_enum(el["min_speed"]["unit"], ["mph", "km/h"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_between_linear_range_length_check() -> Check:
    return Check(
        field="speed_limits[].between",
        name="linear_range_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_linear_range_length(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_between_linear_range_bounds_check() -> Check:
    return Check(
        field="speed_limits[].between",
        name="linear_range_bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_linear_range_bounds(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_between_linear_range_order_check() -> Check:
    return Check(
        field="speed_limits[].between",
        name="linear_range_order",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_linear_range_order(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_heading_check() -> Check:
    return Check(
        field="speed_limits[].when.heading",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: check_enum(el["when"]["heading"], ["forward", "backward"]),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_mode_min_length_check() -> Check:
    return Check(
        field="speed_limits[].when.mode_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_array_min_length(el["when"]["mode"], 1)
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_mode_unique_check() -> Check:
    return Check(
        field="speed_limits[].when.mode_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_struct_unique(el["when"]["mode"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_mode_check() -> Check:
    return Check(
        field="speed_limits[].when.mode[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["mode"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "vehicle",
                            "motor_vehicle",
                            "car",
                            "truck",
                            "motorcycle",
                            "foot",
                            "bicycle",
                            "bus",
                            "hgv",
                            "hov",
                            "emergency",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_using_min_length_check() -> Check:
    return Check(
        field="speed_limits[].when.using_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: check_array_min_length(el["when"]["using"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_using_unique_check() -> Check:
    return Check(
        field="speed_limits[].when.using_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_struct_unique(el["when"]["using"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_using_check() -> Check:
    return Check(
        field="speed_limits[].when.using[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["using"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "as_customer",
                            "at_destination",
                            "to_deliver",
                            "to_farm",
                            "for_forestry",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_recognized_min_length_check() -> Check:
    return Check(
        field="speed_limits[].when.recognized_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: check_array_min_length(el["when"]["recognized"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_recognized_unique_check() -> Check:
    return Check(
        field="speed_limits[].when.recognized_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_struct_unique(el["when"]["recognized"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_recognized_check() -> Check:
    return Check(
        field="speed_limits[].when.recognized[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["recognized"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "as_permitted",
                            "as_private",
                            "as_disabled",
                            "as_employee",
                            "as_student",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_min_length_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits",
                lambda el: check_array_min_length(el["when"]["vehicle"], 1),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_unique_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "speed_limits", lambda el: check_struct_unique(el["when"]["vehicle"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_dimension_required_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].dimension",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: check_required(inner["dimension"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_dimension_enum_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].dimension",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: check_enum(
                        inner["dimension"],
                        ["axle_count", "height", "length", "weight", "width"],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_comparison_required_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].comparison",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: check_required(inner["comparison"]),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_comparison_enum_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].comparison",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: check_enum(
                        inner["comparison"],
                        [
                            "greater_than",
                            "greater_than_equal",
                            "equal",
                            "less_than",
                            "less_than_equal",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_value_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["axle_count"]),
                        check_required(inner["value"]),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_value_required_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(
                            ["height", "length", "weight", "width"]
                        ),
                        check_required(inner["value"]),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_value_bounds_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].value",
        name="bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(
                            ["height", "length", "weight", "width"]
                        ),
                        check_bounds(inner["value"], ge=0.0),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_unit_required_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["height", "length", "width"]),
                        check_required(inner["unit"]),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_unit_enum_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["height", "length", "width"]),
                        check_enum(
                            inner["unit"], ["in", "ft", "yd", "mi", "cm", "m", "km"]
                        ),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_unit_required_check_1() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["weight"]),
                        check_required(inner["unit"]),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_unit_enum_check_1() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            nested_array_check(
                "speed_limits",
                lambda el: array_check(
                    el["when"]["vehicle"],
                    lambda inner: F.when(
                        inner["dimension"].isin(["weight"]),
                        check_enum(
                            inner["unit"], ["oz", "lb", "st", "lt", "g", "kg", "t"]
                        ),
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _subclass_check() -> Check:
    return Check(
        field="subclass",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            check_enum(
                F.col("subclass"),
                [
                    "link",
                    "sidewalk",
                    "crosswalk",
                    "parking_aisle",
                    "driveway",
                    "alley",
                    "cycle_crossing",
                ],
            ),
        ),
        shape=CheckShape.SCALAR,
        root_field="subclass",
    )


def _width_rules_min_length_check() -> Check:
    return Check(
        field="width_rules_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            check_array_min_length(F.col("width_rules"), 1),
        ),
        shape=CheckShape.SCALAR,
        root_field="width_rules",
    )


def _width_rules_unique_check() -> Check:
    return Check(
        field="width_rules_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["road"]), check_struct_unique(F.col("width_rules"))
        ),
        shape=CheckShape.SCALAR,
        root_field="width_rules",
    )


def _width_rules_value_required_check() -> Check:
    return Check(
        field="width_rules[].value",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("width_rules", lambda el: check_required(el["value"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="width_rules",
    )


def _width_rules_value_bounds_check() -> Check:
    return Check(
        field="width_rules[].value",
        name="bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check("width_rules", lambda el: check_bounds(el["value"], gt=0.0)),
        ),
        shape=CheckShape.ARRAY,
        root_field="width_rules",
    )


def _width_rules_between_linear_range_length_check() -> Check:
    return Check(
        field="width_rules[].between",
        name="linear_range_length",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "width_rules", lambda el: check_linear_range_length(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="width_rules",
    )


def _width_rules_between_linear_range_bounds_check() -> Check:
    return Check(
        field="width_rules[].between",
        name="linear_range_bounds",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "width_rules", lambda el: check_linear_range_bounds(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="width_rules",
    )


def _width_rules_between_linear_range_order_check() -> Check:
    return Check(
        field="width_rules[].between",
        name="linear_range_order",
        expr=F.when(
            F.col("subtype").isin(["road"]),
            array_check(
                "width_rules", lambda el: check_linear_range_order(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="width_rules",
    )


def _class_required_check_1() -> Check:
    return Check(
        field="class",
        name="required",
        expr=F.when(F.col("subtype").isin(["rail"]), check_required(F.col("class"))),
        shape=CheckShape.SCALAR,
        root_field="class",
    )


def _class_enum_check_1() -> Check:
    return Check(
        field="class",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            check_enum(
                F.col("class"),
                [
                    "funicular",
                    "light_rail",
                    "monorail",
                    "narrow_gauge",
                    "standard_gauge",
                    "subway",
                    "tram",
                    "unknown",
                ],
            ),
        ),
        shape=CheckShape.SCALAR,
        root_field="class",
    )


def _rail_flags_min_length_check() -> Check:
    return Check(
        field="rail_flags_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            check_array_min_length(F.col("rail_flags"), 1),
        ),
        shape=CheckShape.SCALAR,
        root_field="rail_flags",
    )


def _rail_flags_unique_check() -> Check:
    return Check(
        field="rail_flags_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["rail"]), check_struct_unique(F.col("rail_flags"))
        ),
        shape=CheckShape.SCALAR,
        root_field="rail_flags",
    )


def _rail_flags_values_check() -> Check:
    return Check(
        field="rail_flags[].values",
        name="required",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            array_check("rail_flags", lambda el: check_required(el["values"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="rail_flags",
    )


def _rail_flags_values_min_length_check() -> Check:
    return Check(
        field="rail_flags[].values_min_length",
        name="array_min_length",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            array_check(
                "rail_flags", lambda el: check_array_min_length(el["values"], 1)
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="rail_flags",
    )


def _rail_flags_values_unique_check() -> Check:
    return Check(
        field="rail_flags[].values_unique",
        name="struct_unique",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            array_check("rail_flags", lambda el: check_struct_unique(el["values"])),
        ),
        shape=CheckShape.ARRAY,
        root_field="rail_flags",
    )


def _rail_flags_values_check_1() -> Check:
    return Check(
        field="rail_flags[].values[]",
        name="enum",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            nested_array_check(
                "rail_flags",
                lambda el: array_check(
                    el["values"],
                    lambda inner: check_enum(
                        inner,
                        [
                            "is_bridge",
                            "is_tunnel",
                            "is_under_construction",
                            "is_abandoned",
                            "is_covered",
                            "is_passenger",
                            "is_freight",
                            "is_disused",
                        ],
                    ),
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="rail_flags",
    )


def _rail_flags_between_linear_range_length_check() -> Check:
    return Check(
        field="rail_flags[].between",
        name="linear_range_length",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            array_check(
                "rail_flags", lambda el: check_linear_range_length(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="rail_flags",
    )


def _rail_flags_between_linear_range_bounds_check() -> Check:
    return Check(
        field="rail_flags[].between",
        name="linear_range_bounds",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            array_check(
                "rail_flags", lambda el: check_linear_range_bounds(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="rail_flags",
    )


def _rail_flags_between_linear_range_order_check() -> Check:
    return Check(
        field="rail_flags[].between",
        name="linear_range_order",
        expr=F.when(
            F.col("subtype").isin(["rail"]),
            array_check(
                "rail_flags", lambda el: check_linear_range_order(el["between"])
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="rail_flags",
    )


def _access_restrictions_when_vehicle_check_forbid_if_0_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit_forbidden",
        name="forbid_if",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_forbid_if(
                    inner["unit"],
                    inner["dimension"] == "axle_count",
                    "dimension = 'axle_count'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_check_require_if_1_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit_required_0",
        name="require_if",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "height",
                    "dimension = 'height'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_check_require_if_2_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit_required_1",
        name="require_if",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "length",
                    "dimension = 'length'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_check_require_if_3_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit_required_2",
        name="require_if",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "weight",
                    "dimension = 'weight'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_vehicle_check_require_if_4_check() -> Check:
    return Check(
        field="access_restrictions[].when.vehicle[].unit_required_3",
        name="require_if",
        expr=nested_array_check(
            "access_restrictions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"], inner["dimension"] == "width", "dimension = 'width'"
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _access_restrictions_when_check_require_any_of_5_check() -> Check:
    return Check(
        field="access_restrictions[].when",
        name="require_any_of",
        expr=array_check(
            "access_restrictions",
            lambda el: F.when(
                el["when"].isNotNull(),
                check_require_any_of(
                    [
                        el["when"]["heading"],
                        el["when"]["during"],
                        el["when"]["mode"],
                        el["when"]["using"],
                        el["when"]["recognized"],
                        el["when"]["vehicle"],
                    ],
                    ["heading", "during", "mode", "using", "recognized", "vehicle"],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="access_restrictions",
    )


def _destinations_check_require_any_of_6_check() -> Check:
    return Check(
        field="destinations[]",
        name="require_any_of",
        expr=array_check(
            "destinations",
            lambda el: check_require_any_of(
                [el["labels"], el["symbols"]], ["labels", "symbols"]
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="destinations",
    )


def _prohibited_transitions_when_vehicle_check_forbid_if_7_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit_forbidden",
        name="forbid_if",
        expr=nested_array_check(
            "prohibited_transitions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_forbid_if(
                    inner["unit"],
                    inner["dimension"] == "axle_count",
                    "dimension = 'axle_count'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_check_require_if_8_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit_required_0",
        name="require_if",
        expr=nested_array_check(
            "prohibited_transitions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "height",
                    "dimension = 'height'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_check_require_if_9_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit_required_1",
        name="require_if",
        expr=nested_array_check(
            "prohibited_transitions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "length",
                    "dimension = 'length'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_check_require_if_10_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit_required_2",
        name="require_if",
        expr=nested_array_check(
            "prohibited_transitions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "weight",
                    "dimension = 'weight'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_vehicle_check_require_if_11_check() -> Check:
    return Check(
        field="prohibited_transitions[].when.vehicle[].unit_required_3",
        name="require_if",
        expr=nested_array_check(
            "prohibited_transitions",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"], inner["dimension"] == "width", "dimension = 'width'"
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _prohibited_transitions_when_check_require_any_of_12_check() -> Check:
    return Check(
        field="prohibited_transitions[].when",
        name="require_any_of",
        expr=array_check(
            "prohibited_transitions",
            lambda el: F.when(
                el["when"].isNotNull(),
                check_require_any_of(
                    [
                        el["when"]["heading"],
                        el["when"]["during"],
                        el["when"]["mode"],
                        el["when"]["using"],
                        el["when"]["recognized"],
                        el["when"]["vehicle"],
                    ],
                    ["heading", "during", "mode", "using", "recognized", "vehicle"],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="prohibited_transitions",
    )


def _speed_limits_when_vehicle_check_forbid_if_13_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit_forbidden",
        name="forbid_if",
        expr=nested_array_check(
            "speed_limits",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_forbid_if(
                    inner["unit"],
                    inner["dimension"] == "axle_count",
                    "dimension = 'axle_count'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_check_require_if_14_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit_required_0",
        name="require_if",
        expr=nested_array_check(
            "speed_limits",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "height",
                    "dimension = 'height'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_check_require_if_15_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit_required_1",
        name="require_if",
        expr=nested_array_check(
            "speed_limits",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "length",
                    "dimension = 'length'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_check_require_if_16_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit_required_2",
        name="require_if",
        expr=nested_array_check(
            "speed_limits",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"],
                    inner["dimension"] == "weight",
                    "dimension = 'weight'",
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_vehicle_check_require_if_17_check() -> Check:
    return Check(
        field="speed_limits[].when.vehicle[].unit_required_3",
        name="require_if",
        expr=nested_array_check(
            "speed_limits",
            lambda el: array_check(
                el["when"]["vehicle"],
                lambda inner: check_require_if(
                    inner["unit"], inner["dimension"] == "width", "dimension = 'width'"
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_when_check_require_any_of_18_check() -> Check:
    return Check(
        field="speed_limits[].when",
        name="require_any_of",
        expr=array_check(
            "speed_limits",
            lambda el: F.when(
                el["when"].isNotNull(),
                check_require_any_of(
                    [
                        el["when"]["heading"],
                        el["when"]["during"],
                        el["when"]["mode"],
                        el["when"]["using"],
                        el["when"]["recognized"],
                        el["when"]["vehicle"],
                    ],
                    ["heading", "during", "mode", "using", "recognized", "vehicle"],
                ),
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _speed_limits_check_require_any_of_19_check() -> Check:
    return Check(
        field="speed_limits[]",
        name="require_any_of",
        expr=array_check(
            "speed_limits",
            lambda el: check_require_any_of(
                [el["max_speed"]["value"], el["min_speed"]["value"]],
                ["max_speed.value", "min_speed.value"],
            ),
        ),
        shape=CheckShape.ARRAY,
        root_field="speed_limits",
    )


def _check_forbid_if_20_check() -> Check:
    return Check(
        field="class_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("class"), F.col("subtype") == "water", "subtype = 'water'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_require_if_21_check() -> Check:
    return Check(
        field="class_required_0",
        name="require_if",
        expr=check_require_if(
            F.col("class"), F.col("subtype") == "rail", "subtype = 'rail'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_require_if_22_check() -> Check:
    return Check(
        field="class_required_1",
        name="require_if",
        expr=check_require_if(
            F.col("class"), F.col("subtype") == "road", "subtype = 'road'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_23_check() -> Check:
    return Check(
        field="destinations_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("destinations"), F.col("subtype") != "road", "subtype != 'road'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_24_check() -> Check:
    return Check(
        field="prohibited_transitions_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("prohibited_transitions"),
            F.col("subtype") != "road",
            "subtype != 'road'",
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_25_check() -> Check:
    return Check(
        field="road_flags_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("road_flags"), F.col("subtype") != "road", "subtype != 'road'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_26_check() -> Check:
    return Check(
        field="road_surface_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("road_surface"), F.col("subtype") != "road", "subtype != 'road'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_27_check() -> Check:
    return Check(
        field="speed_limits_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("speed_limits"), F.col("subtype") != "road", "subtype != 'road'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_28_check() -> Check:
    return Check(
        field="subclass_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("subclass"), F.col("subtype") != "road", "subtype != 'road'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_29_check() -> Check:
    return Check(
        field="width_rules_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("width_rules"), F.col("subtype") != "road", "subtype != 'road'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def _check_forbid_if_30_check() -> Check:
    return Check(
        field="rail_flags_forbidden",
        name="forbid_if",
        expr=check_forbid_if(
            F.col("rail_flags"), F.col("subtype") != "rail", "subtype != 'rail'"
        ),
        shape=CheckShape.SCALAR,
        root_field=None,
    )


def segment_checks() -> list[Check]:
    """All validation checks for segment."""
    return [
        _id_required_check(),
        _id_string_min_length_check(),
        _id_no_whitespace_check(),
        _bbox_bbox_completeness_check(),
        _bbox_bbox_lat_ordering_check(),
        _bbox_bbox_lat_range_check(),
        _geometry_required_check(),
        _geometry_geometry_type_check(),
        _theme_required_check(),
        _theme_enum_check(),
        _type_required_check(),
        _type_enum_check(),
        _version_required_check(),
        _version_bounds_check(),
        _sources_min_length_check(),
        _sources_unique_check(),
        _sources_property_required_check(),
        _sources_property_json_pointer_check(),
        _sources_dataset_check(),
        _sources_license_check(),
        _sources_confidence_bounds_check(),
        _sources_confidence_bounds_check_1(),
        _sources_between_linear_range_length_check(),
        _sources_between_linear_range_bounds_check(),
        _sources_between_linear_range_order_check(),
        _subtype_required_check(),
        _subtype_enum_check(),
        _access_restrictions_min_length_check(),
        _access_restrictions_unique_check(),
        _access_restrictions_access_type_required_check(),
        _access_restrictions_access_type_enum_check(),
        _access_restrictions_between_linear_range_length_check(),
        _access_restrictions_between_linear_range_bounds_check(),
        _access_restrictions_between_linear_range_order_check(),
        _access_restrictions_when_heading_check(),
        _access_restrictions_when_mode_min_length_check(),
        _access_restrictions_when_mode_unique_check(),
        _access_restrictions_when_mode_check(),
        _access_restrictions_when_using_min_length_check(),
        _access_restrictions_when_using_unique_check(),
        _access_restrictions_when_using_check(),
        _access_restrictions_when_recognized_min_length_check(),
        _access_restrictions_when_recognized_unique_check(),
        _access_restrictions_when_recognized_check(),
        _access_restrictions_when_vehicle_min_length_check(),
        _access_restrictions_when_vehicle_unique_check(),
        _access_restrictions_when_vehicle_dimension_required_check(),
        _access_restrictions_when_vehicle_dimension_enum_check(),
        _access_restrictions_when_vehicle_comparison_required_check(),
        _access_restrictions_when_vehicle_comparison_enum_check(),
        _access_restrictions_when_vehicle_value_check(),
        _access_restrictions_when_vehicle_value_required_check(),
        _access_restrictions_when_vehicle_value_bounds_check(),
        _access_restrictions_when_vehicle_unit_required_check(),
        _access_restrictions_when_vehicle_unit_enum_check(),
        _access_restrictions_when_vehicle_unit_required_check_1(),
        _access_restrictions_when_vehicle_unit_enum_check_1(),
        _connectors_min_length_check(),
        _connectors_unique_check(),
        _connectors_connector_id_required_check(),
        _connectors_connector_id_string_min_length_check(),
        _connectors_connector_id_no_whitespace_check(),
        _connectors_at_bounds_check(),
        _connectors_at_bounds_check_1(),
        _level_rules_value_check(),
        _level_rules_between_linear_range_length_check(),
        _level_rules_between_linear_range_bounds_check(),
        _level_rules_between_linear_range_order_check(),
        _routes_name_string_min_length_check(),
        _routes_name_stripped_check(),
        _routes_network_string_min_length_check(),
        _routes_network_stripped_check(),
        _routes_ref_string_min_length_check(),
        _routes_ref_stripped_check(),
        _routes_symbol_string_min_length_check(),
        _routes_symbol_stripped_check(),
        _routes_wikidata_check(),
        _routes_between_linear_range_length_check(),
        _routes_between_linear_range_bounds_check(),
        _routes_between_linear_range_order_check(),
        _subclass_rules_value_required_check(),
        _subclass_rules_value_enum_check(),
        _subclass_rules_between_linear_range_length_check(),
        _subclass_rules_between_linear_range_bounds_check(),
        _subclass_rules_between_linear_range_order_check(),
        _names_primary_required_check(),
        _names_primary_string_min_length_check(),
        _names_primary_stripped_check(),
        _names_rules_value_required_check(),
        _names_rules_value_string_min_length_check(),
        _names_rules_value_stripped_check(),
        _names_rules_variant_required_check(),
        _names_rules_variant_enum_check(),
        _names_rules_language_check(),
        _names_rules_perspectives_mode_required_check(),
        _names_rules_perspectives_mode_enum_check(),
        _names_rules_perspectives_countries_check(),
        _names_rules_perspectives_countries_min_length_check(),
        _names_rules_perspectives_countries_unique_check(),
        _names_rules_perspectives_countries_check_1(),
        _names_rules_between_linear_range_length_check(),
        _names_rules_between_linear_range_bounds_check(),
        _names_rules_between_linear_range_order_check(),
        _names_rules_side_check(),
        _class_required_check(),
        _class_enum_check(),
        _destinations_from_connector_id_required_check(),
        _destinations_from_connector_id_string_min_length_check(),
        _destinations_from_connector_id_no_whitespace_check(),
        _destinations_to_connector_id_required_check(),
        _destinations_to_connector_id_string_min_length_check(),
        _destinations_to_connector_id_no_whitespace_check(),
        _destinations_to_segment_id_required_check(),
        _destinations_to_segment_id_string_min_length_check(),
        _destinations_to_segment_id_no_whitespace_check(),
        _destinations_final_heading_required_check(),
        _destinations_final_heading_enum_check(),
        _destinations_labels_min_length_check(),
        _destinations_labels_unique_check(),
        _destinations_labels_value_required_check(),
        _destinations_labels_value_string_min_length_check(),
        _destinations_labels_value_stripped_check(),
        _destinations_labels_type_required_check(),
        _destinations_labels_type_enum_check(),
        _destinations_symbols_unique_check(),
        _destinations_symbols_check(),
        _destinations_when_heading_required_check(),
        _destinations_when_heading_enum_check(),
        _prohibited_transitions_sequence_check(),
        _prohibited_transitions_sequence_min_length_check(),
        _prohibited_transitions_sequence_unique_check(),
        _prohibited_transitions_sequence_connector_id_required_check(),
        _prohibited_transitions_sequence_connector_id_string_min_length_check(),
        _prohibited_transitions_sequence_connector_id_no_whitespace_check(),
        _prohibited_transitions_sequence_segment_id_required_check(),
        _prohibited_transitions_sequence_segment_id_string_min_length_check(),
        _prohibited_transitions_sequence_segment_id_no_whitespace_check(),
        _prohibited_transitions_final_heading_required_check(),
        _prohibited_transitions_final_heading_enum_check(),
        _prohibited_transitions_between_linear_range_length_check(),
        _prohibited_transitions_between_linear_range_bounds_check(),
        _prohibited_transitions_between_linear_range_order_check(),
        _prohibited_transitions_when_heading_check(),
        _prohibited_transitions_when_mode_min_length_check(),
        _prohibited_transitions_when_mode_unique_check(),
        _prohibited_transitions_when_mode_check(),
        _prohibited_transitions_when_using_min_length_check(),
        _prohibited_transitions_when_using_unique_check(),
        _prohibited_transitions_when_using_check(),
        _prohibited_transitions_when_recognized_min_length_check(),
        _prohibited_transitions_when_recognized_unique_check(),
        _prohibited_transitions_when_recognized_check(),
        _prohibited_transitions_when_vehicle_min_length_check(),
        _prohibited_transitions_when_vehicle_unique_check(),
        _prohibited_transitions_when_vehicle_dimension_required_check(),
        _prohibited_transitions_when_vehicle_dimension_enum_check(),
        _prohibited_transitions_when_vehicle_comparison_required_check(),
        _prohibited_transitions_when_vehicle_comparison_enum_check(),
        _prohibited_transitions_when_vehicle_value_check(),
        _prohibited_transitions_when_vehicle_value_required_check(),
        _prohibited_transitions_when_vehicle_value_bounds_check(),
        _prohibited_transitions_when_vehicle_unit_required_check(),
        _prohibited_transitions_when_vehicle_unit_enum_check(),
        _prohibited_transitions_when_vehicle_unit_required_check_1(),
        _prohibited_transitions_when_vehicle_unit_enum_check_1(),
        _road_flags_min_length_check(),
        _road_flags_unique_check(),
        _road_flags_values_check(),
        _road_flags_values_min_length_check(),
        _road_flags_values_unique_check(),
        _road_flags_values_check_1(),
        _road_flags_between_linear_range_length_check(),
        _road_flags_between_linear_range_bounds_check(),
        _road_flags_between_linear_range_order_check(),
        _road_surface_min_length_check(),
        _road_surface_unique_check(),
        _road_surface_value_required_check(),
        _road_surface_value_enum_check(),
        _road_surface_between_linear_range_length_check(),
        _road_surface_between_linear_range_bounds_check(),
        _road_surface_between_linear_range_order_check(),
        _speed_limits_min_length_check(),
        _speed_limits_unique_check(),
        _speed_limits_max_speed_value_required_check(),
        _speed_limits_max_speed_value_bounds_check(),
        _speed_limits_max_speed_value_bounds_check_1(),
        _speed_limits_max_speed_unit_required_check(),
        _speed_limits_max_speed_unit_enum_check(),
        _speed_limits_min_speed_value_required_check(),
        _speed_limits_min_speed_value_bounds_check(),
        _speed_limits_min_speed_value_bounds_check_1(),
        _speed_limits_min_speed_unit_required_check(),
        _speed_limits_min_speed_unit_enum_check(),
        _speed_limits_between_linear_range_length_check(),
        _speed_limits_between_linear_range_bounds_check(),
        _speed_limits_between_linear_range_order_check(),
        _speed_limits_when_heading_check(),
        _speed_limits_when_mode_min_length_check(),
        _speed_limits_when_mode_unique_check(),
        _speed_limits_when_mode_check(),
        _speed_limits_when_using_min_length_check(),
        _speed_limits_when_using_unique_check(),
        _speed_limits_when_using_check(),
        _speed_limits_when_recognized_min_length_check(),
        _speed_limits_when_recognized_unique_check(),
        _speed_limits_when_recognized_check(),
        _speed_limits_when_vehicle_min_length_check(),
        _speed_limits_when_vehicle_unique_check(),
        _speed_limits_when_vehicle_dimension_required_check(),
        _speed_limits_when_vehicle_dimension_enum_check(),
        _speed_limits_when_vehicle_comparison_required_check(),
        _speed_limits_when_vehicle_comparison_enum_check(),
        _speed_limits_when_vehicle_value_check(),
        _speed_limits_when_vehicle_value_required_check(),
        _speed_limits_when_vehicle_value_bounds_check(),
        _speed_limits_when_vehicle_unit_required_check(),
        _speed_limits_when_vehicle_unit_enum_check(),
        _speed_limits_when_vehicle_unit_required_check_1(),
        _speed_limits_when_vehicle_unit_enum_check_1(),
        _subclass_check(),
        _width_rules_min_length_check(),
        _width_rules_unique_check(),
        _width_rules_value_required_check(),
        _width_rules_value_bounds_check(),
        _width_rules_between_linear_range_length_check(),
        _width_rules_between_linear_range_bounds_check(),
        _width_rules_between_linear_range_order_check(),
        _class_required_check_1(),
        _class_enum_check_1(),
        _rail_flags_min_length_check(),
        _rail_flags_unique_check(),
        _rail_flags_values_check(),
        _rail_flags_values_min_length_check(),
        _rail_flags_values_unique_check(),
        _rail_flags_values_check_1(),
        _rail_flags_between_linear_range_length_check(),
        _rail_flags_between_linear_range_bounds_check(),
        _rail_flags_between_linear_range_order_check(),
        _access_restrictions_when_vehicle_check_forbid_if_0_check(),
        _access_restrictions_when_vehicle_check_require_if_1_check(),
        _access_restrictions_when_vehicle_check_require_if_2_check(),
        _access_restrictions_when_vehicle_check_require_if_3_check(),
        _access_restrictions_when_vehicle_check_require_if_4_check(),
        _access_restrictions_when_check_require_any_of_5_check(),
        _destinations_check_require_any_of_6_check(),
        _prohibited_transitions_when_vehicle_check_forbid_if_7_check(),
        _prohibited_transitions_when_vehicle_check_require_if_8_check(),
        _prohibited_transitions_when_vehicle_check_require_if_9_check(),
        _prohibited_transitions_when_vehicle_check_require_if_10_check(),
        _prohibited_transitions_when_vehicle_check_require_if_11_check(),
        _prohibited_transitions_when_check_require_any_of_12_check(),
        _speed_limits_when_vehicle_check_forbid_if_13_check(),
        _speed_limits_when_vehicle_check_require_if_14_check(),
        _speed_limits_when_vehicle_check_require_if_15_check(),
        _speed_limits_when_vehicle_check_require_if_16_check(),
        _speed_limits_when_vehicle_check_require_if_17_check(),
        _speed_limits_when_check_require_any_of_18_check(),
        _speed_limits_check_require_any_of_19_check(),
        _check_forbid_if_20_check(),
        _check_require_if_21_check(),
        _check_require_if_22_check(),
        _check_forbid_if_23_check(),
        _check_forbid_if_24_check(),
        _check_forbid_if_25_check(),
        _check_forbid_if_26_check(),
        _check_forbid_if_27_check(),
        _check_forbid_if_28_check(),
        _check_forbid_if_29_check(),
        _check_forbid_if_30_check(),
    ]


SEGMENT_SCHEMA = StructType(
    [
        StructField("id", StringType(), True),
        StructField("bbox", BBOX_STRUCT, True),
        StructField("geometry", BinaryType(), True),
        StructField("theme", StringType(), True),
        StructField("type", StringType(), True),
        StructField("version", IntegerType(), True),
        StructField(
            "sources",
            ArrayType(
                StructType(
                    [
                        StructField("property", StringType(), True),
                        StructField("dataset", StringType(), True),
                        StructField("license", StringType(), True),
                        StructField("record_id", StringType(), True),
                        StructField("update_time", StringType(), True),
                        StructField("confidence", DoubleType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField("subtype", StringType(), True),
        StructField(
            "access_restrictions",
            ArrayType(
                StructType(
                    [
                        StructField("access_type", StringType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                        StructField(
                            "when",
                            StructType(
                                [
                                    StructField("heading", StringType(), True),
                                    StructField("during", StringType(), True),
                                    StructField(
                                        "mode", ArrayType(StringType(), True), True
                                    ),
                                    StructField(
                                        "using", ArrayType(StringType(), True), True
                                    ),
                                    StructField(
                                        "recognized",
                                        ArrayType(StringType(), True),
                                        True,
                                    ),
                                    StructField(
                                        "vehicle",
                                        ArrayType(
                                            StructType(
                                                [
                                                    StructField(
                                                        "dimension", StringType(), True
                                                    ),
                                                    StructField(
                                                        "comparison", StringType(), True
                                                    ),
                                                    StructField(
                                                        "value", DoubleType(), True
                                                    ),
                                                    StructField(
                                                        "unit", StringType(), True
                                                    ),
                                                ]
                                            ),
                                            True,
                                        ),
                                        True,
                                    ),
                                ]
                            ),
                            True,
                        ),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "connectors",
            ArrayType(
                StructType(
                    [
                        StructField("connector_id", StringType(), True),
                        StructField("at", DoubleType(), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "level_rules",
            ArrayType(
                StructType(
                    [
                        StructField("value", IntegerType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "routes",
            ArrayType(
                StructType(
                    [
                        StructField("name", StringType(), True),
                        StructField("network", StringType(), True),
                        StructField("ref", StringType(), True),
                        StructField("symbol", StringType(), True),
                        StructField("wikidata", StringType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "subclass_rules",
            ArrayType(
                StructType(
                    [
                        StructField("value", StringType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "names",
            StructType(
                [
                    StructField("primary", StringType(), True),
                    StructField(
                        "common", MapType(StringType(), StringType(), True), True
                    ),
                    StructField(
                        "rules",
                        ArrayType(
                            StructType(
                                [
                                    StructField("value", StringType(), True),
                                    StructField("variant", StringType(), True),
                                    StructField("language", StringType(), True),
                                    StructField(
                                        "perspectives",
                                        StructType(
                                            [
                                                StructField("mode", StringType(), True),
                                                StructField(
                                                    "countries",
                                                    ArrayType(StringType(), True),
                                                    True,
                                                ),
                                            ]
                                        ),
                                        True,
                                    ),
                                    StructField(
                                        "between", ArrayType(DoubleType(), True), True
                                    ),
                                    StructField("side", StringType(), True),
                                ]
                            ),
                            True,
                        ),
                        True,
                    ),
                ]
            ),
            True,
        ),
        StructField("class", StringType(), True),
        StructField(
            "destinations",
            ArrayType(
                StructType(
                    [
                        StructField("from_connector_id", StringType(), True),
                        StructField("to_connector_id", StringType(), True),
                        StructField("to_segment_id", StringType(), True),
                        StructField("final_heading", StringType(), True),
                        StructField(
                            "labels",
                            ArrayType(
                                StructType(
                                    [
                                        StructField("value", StringType(), True),
                                        StructField("type", StringType(), True),
                                    ]
                                ),
                                True,
                            ),
                            True,
                        ),
                        StructField("symbols", ArrayType(StringType(), True), True),
                        StructField(
                            "when",
                            StructType([StructField("heading", StringType(), True)]),
                            True,
                        ),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "prohibited_transitions",
            ArrayType(
                StructType(
                    [
                        StructField(
                            "sequence",
                            ArrayType(
                                StructType(
                                    [
                                        StructField("connector_id", StringType(), True),
                                        StructField("segment_id", StringType(), True),
                                    ]
                                ),
                                True,
                            ),
                            True,
                        ),
                        StructField("final_heading", StringType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                        StructField(
                            "when",
                            StructType(
                                [
                                    StructField("heading", StringType(), True),
                                    StructField("during", StringType(), True),
                                    StructField(
                                        "mode", ArrayType(StringType(), True), True
                                    ),
                                    StructField(
                                        "using", ArrayType(StringType(), True), True
                                    ),
                                    StructField(
                                        "recognized",
                                        ArrayType(StringType(), True),
                                        True,
                                    ),
                                    StructField(
                                        "vehicle",
                                        ArrayType(
                                            StructType(
                                                [
                                                    StructField(
                                                        "dimension", StringType(), True
                                                    ),
                                                    StructField(
                                                        "comparison", StringType(), True
                                                    ),
                                                    StructField(
                                                        "value", DoubleType(), True
                                                    ),
                                                    StructField(
                                                        "unit", StringType(), True
                                                    ),
                                                ]
                                            ),
                                            True,
                                        ),
                                        True,
                                    ),
                                ]
                            ),
                            True,
                        ),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "road_flags",
            ArrayType(
                StructType(
                    [
                        StructField("values", ArrayType(StringType(), True), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "road_surface",
            ArrayType(
                StructType(
                    [
                        StructField("value", StringType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "speed_limits",
            ArrayType(
                StructType(
                    [
                        StructField(
                            "max_speed",
                            StructType(
                                [
                                    StructField("value", IntegerType(), True),
                                    StructField("unit", StringType(), True),
                                ]
                            ),
                            True,
                        ),
                        StructField(
                            "min_speed",
                            StructType(
                                [
                                    StructField("value", IntegerType(), True),
                                    StructField("unit", StringType(), True),
                                ]
                            ),
                            True,
                        ),
                        StructField("is_max_speed_variable", BooleanType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                        StructField(
                            "when",
                            StructType(
                                [
                                    StructField("heading", StringType(), True),
                                    StructField("during", StringType(), True),
                                    StructField(
                                        "mode", ArrayType(StringType(), True), True
                                    ),
                                    StructField(
                                        "using", ArrayType(StringType(), True), True
                                    ),
                                    StructField(
                                        "recognized",
                                        ArrayType(StringType(), True),
                                        True,
                                    ),
                                    StructField(
                                        "vehicle",
                                        ArrayType(
                                            StructType(
                                                [
                                                    StructField(
                                                        "dimension", StringType(), True
                                                    ),
                                                    StructField(
                                                        "comparison", StringType(), True
                                                    ),
                                                    StructField(
                                                        "value", DoubleType(), True
                                                    ),
                                                    StructField(
                                                        "unit", StringType(), True
                                                    ),
                                                ]
                                            ),
                                            True,
                                        ),
                                        True,
                                    ),
                                ]
                            ),
                            True,
                        ),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField("subclass", StringType(), True),
        StructField(
            "width_rules",
            ArrayType(
                StructType(
                    [
                        StructField("value", DoubleType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "rail_flags",
            ArrayType(
                StructType(
                    [
                        StructField("values", ArrayType(StringType(), True), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
    ]
)

GEOMETRY_TYPES: tuple[GeometryType, ...] = (GeometryType.LINE_STRING,)

ENTRY_POINT = "overture.schema.transportation:Segment"

PARTITIONS: dict[str, str] = {"theme": "transportation"}

FEATURE_VALIDATION = FeatureValidation(
    schema=SEGMENT_SCHEMA,
    checks=segment_checks,
    geometry_types=GEOMETRY_TYPES,
)
