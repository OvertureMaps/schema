# This file is auto-generated by overture-schema-codegen. Do not edit.

"""Address validation expression builders."""

from __future__ import annotations

from pyspark.sql import functions as F
from pyspark.sql.types import (
    ArrayType,
    BinaryType,
    DoubleType,
    IntegerType,
    StringType,
    StructField,
    StructType,
)

from overture.schema.pyspark.check import Check, CheckShape, FeatureValidation
from overture.schema.pyspark.expressions._schema_structs import (
    BBOX_STRUCT,
)
from overture.schema.pyspark.expressions.column_patterns import (
    array_check,
    check_struct_unique,
)
from overture.schema.pyspark.expressions.constraint_expressions import (
    check_array_max_length,
    check_array_min_length,
    check_bbox_completeness,
    check_bbox_lat_ordering,
    check_bbox_lat_range,
    check_bounds,
    check_enum,
    check_geometry_type,
    check_json_pointer,
    check_linear_range_bounds,
    check_linear_range_length,
    check_linear_range_order,
    check_pattern,
    check_required,
    check_string_min_length,
    check_stripped,
)
from overture.schema.system.primitive import GeometryType


def _id_required_check() -> Check:
    return Check(
        field="id",
        name="required",
        expr=check_required(F.col("id")),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _id_string_min_length_check() -> Check:
    return Check(
        field="id",
        name="string_min_length",
        expr=check_string_min_length(F.col("id"), 1),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _id_no_whitespace_check() -> Check:
    return Check(
        field="id",
        name="no_whitespace",
        expr=check_pattern(
            F.col("id"), "^\\S+\\z", label="String without whitespace characters"
        ),
        shape=CheckShape.SCALAR,
        root_field="id",
    )


def _bbox_bbox_completeness_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_completeness",
        expr=check_bbox_completeness(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _bbox_bbox_lat_ordering_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_lat_ordering",
        expr=check_bbox_lat_ordering(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _bbox_bbox_lat_range_check() -> Check:
    return Check(
        field="bbox",
        name="bbox_lat_range",
        expr=check_bbox_lat_range(F.col("bbox")),
        shape=CheckShape.SCALAR,
        root_field="bbox",
    )


def _geometry_required_check() -> Check:
    return Check(
        field="geometry",
        name="required",
        expr=check_required(F.col("geometry")),
        shape=CheckShape.SCALAR,
        root_field="geometry",
    )


def _geometry_geometry_type_check() -> Check:
    return Check(
        field="geometry",
        name="geometry_type",
        expr=check_geometry_type(F.col("geometry"), GeometryType.POINT),
        shape=CheckShape.SCALAR,
        root_field="geometry",
    )


def _theme_required_check() -> Check:
    return Check(
        field="theme",
        name="required",
        expr=check_required(F.col("theme")),
        shape=CheckShape.SCALAR,
        root_field="theme",
    )


def _theme_enum_check() -> Check:
    return Check(
        field="theme",
        name="enum",
        expr=check_enum(F.col("theme"), ["addresses"]),
        shape=CheckShape.SCALAR,
        root_field="theme",
    )


def _type_required_check() -> Check:
    return Check(
        field="type",
        name="required",
        expr=check_required(F.col("type")),
        shape=CheckShape.SCALAR,
        root_field="type",
    )


def _type_enum_check() -> Check:
    return Check(
        field="type",
        name="enum",
        expr=check_enum(F.col("type"), ["address"]),
        shape=CheckShape.SCALAR,
        root_field="type",
    )


def _version_required_check() -> Check:
    return Check(
        field="version",
        name="required",
        expr=check_required(F.col("version")),
        shape=CheckShape.SCALAR,
        root_field="version",
    )


def _version_bounds_check() -> Check:
    return Check(
        field="version",
        name="bounds",
        expr=check_bounds(F.col("version"), ge=0),
        shape=CheckShape.SCALAR,
        root_field="version",
    )


def _sources_min_length_check() -> Check:
    return Check(
        field="sources_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("sources"), 1),
        shape=CheckShape.SCALAR,
        root_field="sources",
    )


def _sources_unique_check() -> Check:
    return Check(
        field="sources_unique",
        name="struct_unique",
        expr=check_struct_unique(F.col("sources")),
        shape=CheckShape.SCALAR,
        root_field="sources",
    )


def _sources_property_required_check() -> Check:
    return Check(
        field="sources[].property",
        name="required",
        expr=array_check("sources", lambda el: check_required(el["property"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_property_json_pointer_check() -> Check:
    return Check(
        field="sources[].property",
        name="json_pointer",
        expr=array_check("sources", lambda el: check_json_pointer(el["property"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_dataset_check() -> Check:
    return Check(
        field="sources[].dataset",
        name="required",
        expr=array_check("sources", lambda el: check_required(el["dataset"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_license_check() -> Check:
    return Check(
        field="sources[].license",
        name="stripped",
        expr=array_check("sources", lambda el: check_stripped(el["license"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_confidence_bounds_check() -> Check:
    return Check(
        field="sources[].confidence",
        name="bounds",
        expr=array_check("sources", lambda el: check_bounds(el["confidence"], ge=0.0)),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_confidence_bounds_check_1() -> Check:
    return Check(
        field="sources[].confidence",
        name="bounds",
        expr=array_check("sources", lambda el: check_bounds(el["confidence"], le=1.0)),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_length_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_length",
        expr=array_check(
            "sources", lambda el: check_linear_range_length(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_bounds_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_bounds",
        expr=array_check(
            "sources", lambda el: check_linear_range_bounds(el["between"])
        ),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _sources_between_linear_range_order_check() -> Check:
    return Check(
        field="sources[].between",
        name="linear_range_order",
        expr=array_check("sources", lambda el: check_linear_range_order(el["between"])),
        shape=CheckShape.ARRAY,
        root_field="sources",
    )


def _address_levels_min_length_check() -> Check:
    return Check(
        field="address_levels_min_length",
        name="array_min_length",
        expr=check_array_min_length(F.col("address_levels"), 1),
        shape=CheckShape.SCALAR,
        root_field="address_levels",
    )


def _address_levels_max_length_check() -> Check:
    return Check(
        field="address_levels_max_length",
        name="array_max_length",
        expr=check_array_max_length(F.col("address_levels"), 5),
        shape=CheckShape.SCALAR,
        root_field="address_levels",
    )


def _address_levels_value_string_min_length_check() -> Check:
    return Check(
        field="address_levels[].value",
        name="string_min_length",
        expr=array_check(
            "address_levels", lambda el: check_string_min_length(el["value"], 1)
        ),
        shape=CheckShape.ARRAY,
        root_field="address_levels",
    )


def _address_levels_value_stripped_check() -> Check:
    return Check(
        field="address_levels[].value",
        name="stripped",
        expr=array_check("address_levels", lambda el: check_stripped(el["value"])),
        shape=CheckShape.ARRAY,
        root_field="address_levels",
    )


def _country_required_check() -> Check:
    return Check(
        field="country",
        name="required",
        expr=check_required(F.col("country")),
        shape=CheckShape.SCALAR,
        root_field="country",
    )


def _country_country_code_alpha2_check() -> Check:
    return Check(
        field="country",
        name="country_code_alpha2",
        expr=check_pattern(
            F.col("country"), "^[A-Z]{2}\\z", label="ISO 3166-1 alpha-2 country code"
        ),
        shape=CheckShape.SCALAR,
        root_field="country",
    )


def _number_string_min_length_check() -> Check:
    return Check(
        field="number",
        name="string_min_length",
        expr=check_string_min_length(F.col("number"), 1),
        shape=CheckShape.SCALAR,
        root_field="number",
    )


def _number_stripped_check() -> Check:
    return Check(
        field="number",
        name="stripped",
        expr=check_stripped(F.col("number")),
        shape=CheckShape.SCALAR,
        root_field="number",
    )


def _postal_city_string_min_length_check() -> Check:
    return Check(
        field="postal_city",
        name="string_min_length",
        expr=check_string_min_length(F.col("postal_city"), 1),
        shape=CheckShape.SCALAR,
        root_field="postal_city",
    )


def _postal_city_stripped_check() -> Check:
    return Check(
        field="postal_city",
        name="stripped",
        expr=check_stripped(F.col("postal_city")),
        shape=CheckShape.SCALAR,
        root_field="postal_city",
    )


def _postcode_string_min_length_check() -> Check:
    return Check(
        field="postcode",
        name="string_min_length",
        expr=check_string_min_length(F.col("postcode"), 1),
        shape=CheckShape.SCALAR,
        root_field="postcode",
    )


def _postcode_stripped_check() -> Check:
    return Check(
        field="postcode",
        name="stripped",
        expr=check_stripped(F.col("postcode")),
        shape=CheckShape.SCALAR,
        root_field="postcode",
    )


def _street_string_min_length_check() -> Check:
    return Check(
        field="street",
        name="string_min_length",
        expr=check_string_min_length(F.col("street"), 1),
        shape=CheckShape.SCALAR,
        root_field="street",
    )


def _street_stripped_check() -> Check:
    return Check(
        field="street",
        name="stripped",
        expr=check_stripped(F.col("street")),
        shape=CheckShape.SCALAR,
        root_field="street",
    )


def _unit_string_min_length_check() -> Check:
    return Check(
        field="unit",
        name="string_min_length",
        expr=check_string_min_length(F.col("unit"), 1),
        shape=CheckShape.SCALAR,
        root_field="unit",
    )


def _unit_stripped_check() -> Check:
    return Check(
        field="unit",
        name="stripped",
        expr=check_stripped(F.col("unit")),
        shape=CheckShape.SCALAR,
        root_field="unit",
    )


def address_checks() -> list[Check]:
    """All validation checks for address."""
    return [
        _id_required_check(),
        _id_string_min_length_check(),
        _id_no_whitespace_check(),
        _bbox_bbox_completeness_check(),
        _bbox_bbox_lat_ordering_check(),
        _bbox_bbox_lat_range_check(),
        _geometry_required_check(),
        _geometry_geometry_type_check(),
        _theme_required_check(),
        _theme_enum_check(),
        _type_required_check(),
        _type_enum_check(),
        _version_required_check(),
        _version_bounds_check(),
        _sources_min_length_check(),
        _sources_unique_check(),
        _sources_property_required_check(),
        _sources_property_json_pointer_check(),
        _sources_dataset_check(),
        _sources_license_check(),
        _sources_confidence_bounds_check(),
        _sources_confidence_bounds_check_1(),
        _sources_between_linear_range_length_check(),
        _sources_between_linear_range_bounds_check(),
        _sources_between_linear_range_order_check(),
        _address_levels_min_length_check(),
        _address_levels_max_length_check(),
        _address_levels_value_string_min_length_check(),
        _address_levels_value_stripped_check(),
        _country_required_check(),
        _country_country_code_alpha2_check(),
        _number_string_min_length_check(),
        _number_stripped_check(),
        _postal_city_string_min_length_check(),
        _postal_city_stripped_check(),
        _postcode_string_min_length_check(),
        _postcode_stripped_check(),
        _street_string_min_length_check(),
        _street_stripped_check(),
        _unit_string_min_length_check(),
        _unit_stripped_check(),
    ]


ADDRESS_SCHEMA = StructType(
    [
        StructField("id", StringType(), True),
        StructField("bbox", BBOX_STRUCT, True),
        StructField("geometry", BinaryType(), True),
        StructField("theme", StringType(), True),
        StructField("type", StringType(), True),
        StructField("version", IntegerType(), True),
        StructField(
            "sources",
            ArrayType(
                StructType(
                    [
                        StructField("property", StringType(), True),
                        StructField("dataset", StringType(), True),
                        StructField("license", StringType(), True),
                        StructField("record_id", StringType(), True),
                        StructField("update_time", StringType(), True),
                        StructField("confidence", DoubleType(), True),
                        StructField("between", ArrayType(DoubleType(), True), True),
                    ]
                ),
                True,
            ),
            True,
        ),
        StructField(
            "address_levels",
            ArrayType(StructType([StructField("value", StringType(), True)]), True),
            True,
        ),
        StructField("country", StringType(), True),
        StructField("number", StringType(), True),
        StructField("postal_city", StringType(), True),
        StructField("postcode", StringType(), True),
        StructField("street", StringType(), True),
        StructField("unit", StringType(), True),
    ]
)

GEOMETRY_TYPES: tuple[GeometryType, ...] = (GeometryType.POINT,)

ENTRY_POINT = "overture.schema.addresses:Address"

PARTITIONS: dict[str, str] = {"theme": "addresses"}

FEATURE_VALIDATION = FeatureValidation(
    schema=ADDRESS_SCHEMA,
    checks=address_checks,
    geometry_types=GEOMETRY_TYPES,
)
