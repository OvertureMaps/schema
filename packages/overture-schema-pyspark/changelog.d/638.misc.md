Moved expression-generation from the shared prebuild step into the package's own `scripts/prebuild.sh`, run by `release-publish.yaml` before `uv build`. CI-only change: no runtime code, dependencies, or wheel contents affected.
